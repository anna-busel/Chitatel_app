# AUDIT-2026-07 — ЧИТАТЕЛЬ: аудит архитектуры, App Store compliance и безопасности

> **Дата аудита:** 07.07.2026. Аудировался код в `main` (sha файлов на эту дату), НЕ галочки в доках (урок #49).
> **Назначение:** живой чек-лист внедрения. Каждый пункт закрывать сменой ⬜ → ✅ с датой и коммитом.
> **Как использовать AI:** при работе над Фазой 6 (полировка), тестом покупок и подготовкой к сабмиту — читать этот файл ПОСЛЕ AI-CONTEXT-3. Статусы задач — здесь; правила и роль — в Project Instructions.
>
> **⚠️ ОБНОВЛЕНО 08.07.2026:** блок B (платёжный контур) — B2/B3/B4 ЗАКРЫТЫ; логика доступа к клубу переработана (календарная модель + письмо в архиве); тест покупок пройден вживую (monthly, sandbox).
>
> **🎉 ОБНОВЛЕНО 12.07.2026: БЛОК A ЗАКРЫТ ПОЛНОСТЬЮ (кроме A7 — его делает Анна руками в ASC).** Фаза 6, волна 6А. Детали — журнал внизу и AI-CONTEXT-3.
> **⚠️ ВАЖНО: код написан, но НЕ ПРОВЕРЕН В БИЛДЕ.** Пункты помечены ✅ по факту написания кода. После общего билда пройти чек-лист проверок (AI-CONTEXT-3 → «ЧТО ПРОВЕРЯТЬ В БИЛДЕ»).

---

## ЧТО БЫЛО ИЗУЧЕНО (граница аудита)

**Прочитано полностью:** AI-CONTEXT / -2 / -3; сервер: `app.js`, `server.js`, `socket/index.js`, `middleware/auth.js`, `middleware/rate-limit.js`, `services/auth.service.js`, `services/purchase.service.js`, `services/webhook.service.js`, `services/image.service.js`, `routes/club.js` (весь), `routes/admin.js`, `routes/webhooks.js`, `routes/auth.js`, `models/User.js`, полный список роутов; клиент: `Info.plist`.

**НЕ покрыто (слепые зоны, аудировать позже отдельно):**
- Flutter-клиент глубоко. **[08.07: paywall прочитан — цены из StoreKit, упоминаний сайта/внешней оплаты НЕТ. book_screen.dart — не аудирован глубоко.]** **[12.07: прочитаны и переписаны chat_tab, qa_tab, home_screen, home_header, app_router, auth_provider/service — см. блок A.]**
- MASTER.md / STEP-BY-STEP.md построчно (код взят как источник истины).
- Фаза 5 (ИИ-дневник) — **[12.07: код написан; compliance закрыт в A3 (OpenAI в Privacy) и A2 (удаление Quote/WeeklyReport). В билде не проверено.]**

---

## БЛОК A — БЛОКЕРЫ САБМИТА ✅ ЗАКРЫТ 12.07.2026 (Фаза 6, волна 6А)

### A1. ✅ 🔴 Блокировка пользователя пользователем (Apple Guideline 1.2) — СДЕЛАНО 12.07
**Было:** `User.js` без `blockedUsers[]`; эндпоинтов block нет; `liveMessagesFilter` не фильтрует; socket-эмит всем; toggle в шторке нет. Функции не существовало ни на сервере, ни на клиенте.

**Сделано:**
- Сервер: `blockedUsers[]` в User; новый роут `routes/users.js` — `POST/DELETE /api/users/:id/block`, `GET /api/users/blocked`, `POST /api/users/:id/report` (жалоба на пользователя, `targetType:'user'`); монтаж в `app.js`.
- `club.js`: фильтр `$nin` по `userId` в `GET /chat` и `GET /chat/context`; **закреп от заблокированной не отдаётся**; **reply-снапшот обнуляется** (иначе её текст протекал через превью ответа — см. урок #60).
- Клиент: `club/services/block_service.dart` (+ `blockedIdsProvider`); `chat_tab.dart` — блокировка **из шторки жалобы (тумблер)**, мгновенное скрытие её сообщений + фильтрация входящих по WebSocket; `profile/screens/blocked_users_screen.dart` — **разблокировка** (Apple требует обратимости).

**Решения:** блок живёт в шторке жалобы (добавлять пункт в меню = переписывать `chat_message_bubble.dart` 50 КБ; Apple такой UX принимает). **Админа заблокировать нельзя** (иначе участница теряет разборы и ответы Анны). WS не персонализируем — фильтруем на клиенте. **Q&A блок-листом НЕ фильтруется** (вопрос публикуется только после ответа Анны = уже прошёл через человека).

⚠️ Осталось: исправить ошибочную галочку MASTER 6.1 п.8 (при следующей правке доков).

### A2. ✅ 🔴 Account deletion + отзыв Apple-токена (Apple 5.1.1(v)) — СДЕЛАНО 12.07
**Было:** серверного флоу нет вовсе; экран висел в воздухе. **Дополнительно найдено:** `authorizationCode` принимался схемой, но НЕ использовался — Apple refresh-токен не сохранялся, отзывать было нечего.

**Сделано:** `DELETE /api/auth/account` (body `{confirm:'УДАЛИТЬ'}`) → revoke у Apple → удаление `Quote` + `WeeklyReport` + `Progress` → стирание PII (имя→«Удалённый аккаунт», `$unset` уникальных полей) → `isDeleted=true`. Обмен `authorizationCode` → `appleRefreshToken` при входе (`apple-auth.service.js` + `User.js` + клиент `auth_service.dart`/`auth_provider.dart`). Экран `delete_account_screen.dart`.

**Решения:** сообщения чата НЕ удаляем (reply-контекст рассыплется) — анонимизируются через имя автора. `Purchase` НЕ удаляем (финансовые записи, PII не содержат).

⚠️ **Revoke работает только у вошедших ЗАНОВО после этого билда** (у старых сессий токена нет — в лог пишется предупреждение, удаление проходит). Требует `.env`: `APPLE_KEY_ID`, `APPLE_PRIVATE_KEY_PATH` (ключ **Sign in with Apple**, не IAP).

### A3. ✅ 🔴 Privacy Policy + Terms/EULA + Support URL — СДЕЛАНО 12.07
`server/public/legal/{privacy,terms,support}.html`, раздача Express'ом (`/legal`, `extensions:['html']`):
- **privacy** — OpenAI назван прямо, согласие и его отзыв, удаление аккаунта описано ровно как в коде, оплата только Apple, «не собираем рекламные идентификаторы»;
- **terms** — **нулевая терпимость к оскорбительному контенту + инструменты (жалоба, блокировка) + разбор жалоб ≤24ч** (обязательный пункт Guideline 1.2 — не удалять при правках); подписки Apple, автопродление, отмена;
- **support** — Support URL для ASC.

Клиент: `core/constants/app_contacts.dart` — почта и ссылки в ОДНОМ месте; paywall и `support_screen` берут оттуда.

⚠️ **ВРЕМЕННЫЕ ДАННЫЕ:** `support@chitatel.app` (ящик НЕ заведён) и «ИП Hanna Busel, Тбилиси, Грузия» (не проверено). Заменить до сабмита. Смена почты = пересборка приложения.
⚠️ Тексты писал разработчик, не юрист — честно описывают поведение кода; показать юристу перед релизом.

### A4. ✅ 🔴 Info.plist — СДЕЛАНО 12.07
Удалён `NSUserTrackingUsageDescription`; честные Photo/Camera («сообщения в чате клуба и фото профиля» — после 6.2 аватар реально есть); добавлен `ITSAppUsesNonExemptEncryption=false`.
⚠️ Первый коммит записал экранированный XML — поймано, исправлено. Файл валиден (1900 байт).

### A5. ✅ 🔴 Админка жалоб (реакция ≤24ч) — СДЕЛАНО 12.07
`server/admin/index.html` — одна страница без сборки, `https://api.chitatel.app/admin`. Вход почтой/паролём (role=admin). Разделы: **жалобы** (скрыть / предупредить / мьют 7 дней / бан / отклонить) и **Q&A** (ответить).

**🔴 РЕШЕНИЕ (расходится со STEP-BY-STEP 6.6):** отдельной React+Vite панели **НЕ будет**. Полноценная админка = ЭТА ЖЕ страница, дорастающая разделами (Книги, Клуб месяца, Аудио, Push, Мысль дня). Причина: сборочный стек не окупается для одного пользователя; переписывать работающее на React — потеря. Поддомен `admin.chitatel.app` — когда панель дорастёт (одна строка nginx).

⚠️ **CSP:** глобальный `helmet` блокировал инлайновый скрипт → страница открывалась ПУСТОЙ. Точечное послабление для `/admin` и `/legal` в `app.js` (урок #62).

### A6. ✅ 🔴 Убрать `_Placeholder` — СДЕЛАНО 12.07
Роутер без единого «Экран в разработке». Профиль настоящий (9 экранов, задача 6.2).
Попутно вырезано мёртвое: **реферал** (баннер на главной + пункт профиля + `Routes.referral`) — обещал «месяц клуба в подарок», механики нет ни в коде, ни на сервере; **колокольчик** на главной — вёл на несуществующую ленту уведомлений (вернём в 6Б с push).

### A7. ⬜ 🔴 Privacy labels в App Store Connect — ДЕЛАЕТ АННА
Заполнить честно: email/имя (аккаунт), **User Content (цитаты, сообщения, фото, голосовые)**, прогресс/использование, покупки. **Обязательно указать передачу данных третьей стороне — OpenAI** (текст цитаты при включённом ИИ-анализе). Трекинга нет, рекламных идентификаторов нет.

---

## 🔴 НОВЫЕ НАХОДКИ 12.07.2026 (при работе над Фазой 6)

### N1. 🔴 `GOOGLE_CLIENT_ID` пуст → вход через Google НЕ РАБОТАЕТ
Сервер верифицирует Google-токен с пустым audience → падение. Кнопка на экране входа есть. **Ревьюер нажмёт и получит ошибку = отклонение.**
**Решение до сабмита:** либо выдать iOS client ID из Google Cloud Console, либо **скрыть кнопку Google** (правка `login_screen.dart`). Google понадобится для Android и сайта — там своя настройка.
Апелляция к Guideline: наличие Google-входа само по себе НЕ нарушение (Sign in with Apple присутствует) — проблема чисто в неработающей кнопке.

### N2. 🔴 `APPLE_ENVIRONMENT=sandbox` в проде
Перед релизом переключить на `production`, иначе верификация покупок у живых юзеров сломается.

### N3. 🔴 Q&A не работал вовсе (эндпоинт без UI) — ИСПРАВЛЕНО 12.07
`POST /api/admin/qa/:id/answer` существовал с задачи 4.4, но **кнопки «Ответить» не было НИГДЕ** — ни в приложении, ни в вебе. Участницам показывалось «Анна отвечает по пятницам», ответить было физически негде (только curl).
Закрыто: кнопка в карточке вопроса под аккаунтом admin (`qa_admin_service.dart` + `qa_tab.dart`) + раздел в админке. **Урок #59: при аудите проверять не «есть ли роут», а «кто и откуда его вызывает».**

### N4. 🟡 Аватар: раньше не загружался вовсе — СДЕЛАНО 12.07
`POST /api/profile/avatar` (multipart), файл в `AUDIO_BASE_PATH/avatars/<userId>/`, signed URL с фиксированным exp (стабильный → кэшируется). Ссылка в `User.avatarUrl` → подставляется автором в каждое сообщение чата → **фото видно напротив реплик, код чата править не пришлось**. Старый файл удаляется при смене.

### N5. 🟡 Статистика прослушивания — только за всё время
`GET /api/progress/stats` (⚠️ объявлен ВЫШЕ `/:bookId`). Недельных минут НЕТ и не будет без посуточной записи: `Progress` хранит только суммарные секунды. **Блок минут в недельном ИИ-отчёте остаётся нулевым** — решение юзера 12.07. Если понадобится — отдельная задача (словарь «дата→секунды» в Progress или новая коллекция).

### N6. 🟡 Регресс ключей в ленте чата — ИСПРАВЛЕНО 12.07
Эксперимент 1 (`85c02c9`) снял ВСЕ ключи из `itemBuilder`, хотя гипотеза касалась только GlobalKey. Без `ValueKey(m.id)` ListView сопоставляет элементы по индексу → при удалении из середины состояние (картинка, голосовое) переиспользуется под чужим индексом. Ключи возвращены. **Никогда не убирать ключи из ленты.**

### N7. 🟡 Node v18 на сервере, `package.json` требует ≥20
`npm install` предупреждает (EBADENGINE). Сейчас работает (`openai` стартует). При странных ошибках вокруг ИИ — обновлять Node до 20 LTS.

### N8. 🟡 Гигиена секретов
`.env` целиком (Mongo-пароль, JWT-секреты, AUDIO_SECRET) и `.p8`-ключ засветились в переписке 12.07. Утечки вовне, скорее всего, нет, но **ротировать секреты после релиза**. Пароль admin-аккаунта (`anna123456`) светится в доках — сменить.

---

## БЛОК B — ПЛАТЁЖНЫЙ КОНТУР ✅ ЗАКРЫТ 07–12.07.2026

### B1. ✅ 🟡 Окружение — СДЕЛАНО 08.07
`.env`: APPLE_*_IAP + root certs Apple PKI + `APPLE_APP_APPLE_ID` + `APPLE_ENVIRONMENT=sandbox` (⚠️ см. N2). Webhook Sandbox URL в ASC. Покупка прошла → verify работает.

### B2. ✅ 🟡 Дыра webhook-маппинга: `appAccountToken` — ИСПРАВЛЕНО 07.07 (`4adfe72`, `f1b3408`, `abd5e75`)
Webhook находил юзера ТОЛЬКО по сохранённому Purchase → продление по неизвестной транзакции (переустановка, family sharing, ASK_TO_BUY) молча терялось. Фикс: клиент передаёт `appAccountToken` (детерминированно из userId), сервер — fallback-маппинг.
⚠️ **Проверка на продлениях (DID_RENEW) в sandbox — ещё не гоняли.**

### B3. ✅ 🟡 `DID_FAIL_TO_RENEW` → grace period — ИСПРАВЛЕНО 07.07 (`4adfe72`, `8c1e133`)

### B4. ✅ 🟡 `subscriptionPlan` enum + `'season'` — ИСПРАВЛЕНО 07.07 (`ca68318`, `8c1e133`)

### B5. ⬜ 🟡 Коды активации — красные линии при реализации
Механизм легален. Держать: нейтральное поле «Активировать код», в приложении НИ СЛОВА «купите на сайте», никаких ссылок на лендинг с ценами. **[08.07: paywall проверен — упоминаний внешней оплаты НЕТ ✅. 12.07: юридические страницы и экран поддержки — тоже без упоминаний внешней покупки ✅.]**

---

## БЛОК B+ — ЛОГИКА ДОСТУПА К КЛУБУ (переработана 08.07.2026)

### ✅ Календарная модель доступа — СДЕЛАНО 08.07
Клуб доступен полноценно (чтение + запись) весь свой календарный месяц + весь СЛЕДУЮЩИЙ (архив). **В архиве МОЖНО ПИСАТЬ.** Привязка к календарю, не к дате платежа. Нет подписки → paywall. Старше окна → только админ. Будущий клуб → read-only анонс подписчику.

### ✅ Paywall-связка (`1f38994`) · ✅ После покупки → сразу в клуб
**[12.07: success_screen УДАЛЁН из потока — paywall сам делает invalidate + `context.go('/club')`.]**

### ✅ Кнопка «Слушать» на книге клуба для подписчика — СДЕЛАНО (`15e5e27`, `a8bd5dd`)

### 🟡 Рассинхрон цен (27.99/34.99) — НЕ баг кода
Storefront-кэш TestFlight-среды. У реальных юзеров один регион. Страховка (перезагрузка продуктов при открытии paywall) — PENDING.

---

## БЛОК C — ПОСЛЕ ПОДАЧИ (v1.0.x–v1.1, НЕ блокеры)

### C1. ⬜ 🟡 Rate limiting на контент-эндпоинты
Limiter ТОЛЬКО на `/api/auth`. POST чата/картинок/реакций/жалоб/Q&A/цитат — без лимитов. **[12.07: добавились `/api/users/:id/block` и `/report` — тоже без лимитов; накрутка жалоб возможна. `/api/quotes` имеет лимит 50/сутки в коде роута.]** Фикс: общий limiter на `/api` (~300/мин/IP) + строгий на upload и report. Webhook НЕ накрывать.

### C2. ⬜ 🟡 `resolveClubAccess` на reaction (и проверка клуба в PATCH/DELETE)
В комментарии кода написано «нужен resolveClubAccess», в цепочке его НЕТ. Любой залогиненный free-юзер реагирует на сообщения любого клуба по ID. **[12.07: при переписывании club.js специально НЕ трогал — это изменение поведения, требует согласования.]**

### C3. ⬜ 🟡 Хэшировать refresh-токены в БД
`user.refreshTokens = [raw JWT]`. Rotation + reuse-detection сделаны правильно — не трогать. **[12.07: добавилось поле `appleRefreshToken` — тоже открытым текстом; отзывной токен Apple, при утечке дампа позволяет отозвать доступ, не более.]**

### C4. ⬜ 🟡 Magic-bytes проверка загрузок
MIME по заголовку клиента. **[12.07: то же для аватара — `POST /profile/avatar` проверяет только `req.file.mimetype`.]**

### C5. ⬜ 🟠 `readBy[]` исключить из выдачи чата.
### C6. ⬜ 🟠 Индексы: `ChatMessage {clubMonthId:1, createdAt:-1}`, `Purchase {transactionId:1}` unique, `Report {status:1, createdAt:-1}`. **[12.07: + `User.blockedUsers` не индексируется — не нужно (читается по `_id` юзера).]**
### C7. ⬜ 🟠 `express.json` limit 10mb → вниз.
### C8. ⬜ 🟠 Аудио через nginx (`X-Accel-Redirect`).
### C9. ⬜ 🟠 Q&A антидубль O(n); reactions read-modify-write (перевести на атомарные `$pull`/`$addToSet`).
### C10. ⬜ 🟠 Гигиена: `.gitignore` (`audio-storage/`, `.env*`); **проверить историю git — если `.env` коммитился, ротация секретов**; см. также N8.

---

## БЛОК D — НЕ ТРОГАТЬ (сделано добротно)

- Socket.io-архитектура. **PM2 строго fork mode, 1 инстанс** (cluster сломает эмиты и **задвоит cron еженедельного отчёта**).
- Signed-URL механика (HMAC, storagePath + перевыпуск при отдаче, фикс-exp 2099 для картинок). **[12.07: та же схема переиспользована для аватаров — стабильный URL нужен, т.к. он попадает в каждое сообщение чата.]**
- Auth-флоу: bcrypt 12, Zod, refresh rotation + reuse-detection, Apple/Google верификация на сервере.
- IAP-верификация: `SignedDataVerifier`, идемпотентный `applyTransaction`, webhook всегда 200.
- Чат: soft-delete + `liveMessagesFilter`, populate reply-снапшотов, sanitizeMentions, антиобход voice/ссылок, path-traversal защита.
- Гостевой режим (5.1.1(v)) — закрыт.

---

## БЛОК E — СИНХРОНИЗАЦИЯ ДОКУМЕНТОВ

Расхождения, требующие правки доков:
1. MASTER 6.1 п.8 — ошибочная галочка block. **[12.07: блокировка теперь РЕАЛЬНО есть — галочка стала правдой, но по случайности.]**
2. Account deletion — в доках «экран есть, проверить», серверного флоу не было. **[12.07: ✅ сделано.]**
3. `NSUserTrackingUsageDescription` стоял ошибочно. **[12.07: ✅ удалён.]**
4. Reaction без resolveClubAccess (C2) — расхождение кода с комментарием. Не исправлено.
5. Usage descriptions не соответствовали факту. **[12.07: ✅ исправлено.]**
6. PM2 fork, 1 инстанс. **[✅ зафиксировано.]**
7. Календарная модель доступа — внести в MASTER (старая запись «21/31 день» устарела).
8. **[НОВОЕ 12.07]** **STEP-BY-STEP 6.6 (React-админка) ОТМЕНЁН** — админка растёт из статической страницы `server/admin/index.html`. Не начинать React-проект.
9. **[НОВОЕ 12.07]** **Реферальная программа (4.41) выпилена** из UI до появления реальной механики. `referral_banner.dart` остался в репо, не импортируется.
10. **[НОВОЕ 12.07]** Лента уведомлений (4.30) отсутствует — колокольчик убран, вернуть вместе с push (6.1).

---

## ЖУРНАЛ ВНЕДРЕНИЯ

| Дата | Пункт | Коммит(ы) | Примечание |
|------|-------|-----------|------------|
| 07.07 | B4 subscriptionPlan enum + 'season' | `ca68318`, `8c1e133` | |
| 07.07 | B3 DID_FAIL_TO_RENEW → grace | `4adfe72`, `8c1e133` | |
| 07.07 | B2 appAccountToken | `4adfe72`, `f1b3408`, `abd5e75` | |
| 08.07 | PM2 fork mode | `45a24d1` | |
| 08.07 | B+ календарная модель клуба | `53b661c`→`022e85f`, `880d6ac`, `827f81c`→`f6fe9ce`, `76f1cb78` | |
| 08.07 | B+ paywall-связка | `a752973`→`1f38994` | |
| 08.07 | B1 окружение (.env + root certs) | — (инфра) | тест покупок monthly пройден |
| 09-12.07 | Апгрейд стека, платежи закрыты живьём, сезоны | см. AI-CONTEXT-3 | monthly + season куплены |
| **12.07** | **Шаг 0: тест-жест сезонов убран** | paywall_screen | диаг-лог `ef07bc7` решено ОСТАВИТЬ (серверный лог, полезен) |
| **12.07** | **A4 Info.plist** | Info.plist (+ фикс XML) | |
| **12.07** | **A1 блокировка (сервер)** | User.js, routes/users.js, app.js, club.js | фильтр в /chat, /context, закрепе, reply-снапшотах |
| **12.07** | **A1 блокировка (клиент)** | api_endpoints, block_service, chat_tab, blocked_users_screen | тумблер в шторке жалобы + WS-фильтр |
| **12.07** | **A2 удаление аккаунта + revoke Apple** | auth.js, apple-auth.service, User.js, auth_service/provider, delete_account_screen | обмен authorizationCode при входе |
| **12.07** | **N3 Q&A из приложения** | qa_admin_service, qa_tab, api_endpoints | эндпоинт был, UI не было нигде |
| **12.07** | **6.2 профиль (сервер)** | profile.js, progress.js, purchases.js, image.service, User.js | + аватар, + GET /progress/stats, + /purchases/history |
| **12.07** | **6.2 профиль (клиент)** | profile_service/provider + 8 экранов, routes.dart | |
| **12.07** | **A6 без заглушек** | app_router, home_screen, home_header | реферал и колокольчик вырезаны |
| **12.07** | **A5 админка** | server/admin/index.html, app.js (CSP) | жалобы + Q&A |
| **12.07** | **A3 legal** | public/legal/{privacy,terms,support}.html, app_contacts.dart, support_screen, paywall | OpenAI + нулевая терпимость |

**Осталось незакрытым:**
- 🔴 **A7 privacy labels в ASC** — делает Анна.
- 🔴 **N1 Google-кнопка** (ключ или скрыть) · **N2 `APPLE_ENVIRONMENT=production`** · рабочая почта поддержки · реквизиты в legal.
- 🟡 Проверка B2 на продлениях (DID_RENEW в sandbox).
- 🟡 **Всё вышеперечисленное написано, но НЕ ПРОВЕРЕНО В БИЛДЕ** (Фазы 5 и 6).
- 🟠 Волна 6Б: push (6.1), онбординг+опрос (6.3), accessibility (6.4), error states (6.5), доращивание админки.
- 🟠 Весь блок C (после релиза).

---

*Создан 07.07.2026. Обновлён 08.07 (блок B закрыт). **Обновлён 12.07.2026: БЛОК A ЗАКРЫТ (кроме A7 — в ASC), добавлены находки N1-N8.** Обновлять статусы ⬜→✅ в той же сессии, что и код. Приоритеты: 🔴 блокер сабмита · 🟡 важно · 🟠 потом.*
