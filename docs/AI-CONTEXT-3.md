# AI-CONTEXT-3 — ЧИТАТЕЛЬ (продолжение)

> **ТРЕТИЙ ФАЙЛ КОНТЕКСТА.** Предыдущие: `AI-CONTEXT.md` (база, Фазы 1-2, чат 4.1-4.8, уроки #1-19) и `AI-CONTEXT-2.md` (Фаза 4, задачи 1.3/1.7/1.8, уроки #20-27).
>
> **Порядок чтения для AI:** `AI-CONTEXT.md` → `AI-CONTEXT-2.md` → **этот файл**.
>
> **Прогресс фиксировать ДАЛЕЕ ТОЛЬКО ЗДЕСЬ.** Первые два не редактировать (архив).
>
> ⚠️ **Юзеру:** добавить упоминание `AI-CONTEXT-3.md` в Project Instructions проекта Claude (чтобы будущие сессии его читали).

---

## ТЕКУЩИЙ СТАТУС (на 26.06.2026)

**✅ ДЕПЛОЙ СЕРВЕРА ВЫПОЛНЕН (26.06.2026).** Бэкенд работает в проде: `https://api.chitatel.app` отдаёт API по HTTPS, каталог залит в свою MongoDB. Подробности — блок «ДЕПЛОЙ СЕРВЕРА — ВЫПОЛНЕНО» ниже. Это снимает БЛОКЕР, который мешал тесту покупок, webhook, кодам и т.д.

**Фаза 3 (Платежи) — КОД ГОТОВ + КАБИНЕТ НАСТРОЕН.** 3.3 (верификация), 3.4 (webhook), 3.2 (Flutter paywall) — закоммичены. 3.1 (кабинет App Store Connect) — сделано Анной/юзером.

**Также ранее:** пересобран каталог (54 разбора + 10 пакетов); согласована модель подписок/доступа под Apple; правило архивного доступа 21 → 31 день.

**Не сделано / блокеры до реального теста покупок (после деплоя сервера осталось):**
- **Ручные шаги 3.2** (pub add, роутер, Xcode capability, ссылки Terms/Privacy) — юзер ещё не делал.
- **`.env` на сервере заполнен НЕ полностью** — пусты `APPLE_*_IAP` (верификация покупок, нужен ASC API key), `GOOGLE_CLIENT_ID`, `OPENAI_API_KEY`, `APNS_*`. Сервер работает, но верификация покупок неполна, пока не впишем ASC-ключ.
- **Sandbox-тестировщики** — не созданы.
- **Webhook URL** в App Store Connect — не прописан (теперь сервер живой → можно прописать `https://api.chitatel.app/api/webhooks/apple`).
- **Тест покупок и Apple Sign In — только на физ.айфоне** (в симуляторе не работают). Анна тестирует удалённо → нужен **Codemagic + TestFlight** (машина 2017 не соберёт Xcode 26 — см. AC-2).

**⚠️ КРИТИЧЕСКОЕ СТРАТЕГИЧЕСКОЕ: основная аудитория — РОССИЯ + БЕЛАРУСЬ + Грузия. Apple-оплата в РФ/РБ НЕ РАБОТАЕТ (карты не принимаются с 2022). Через Apple платит только Грузия/Украина/Казахстан/заграница. Значит для основной массы клиентов главный канал — САЙТ + КОДЫ АКТИВАЦИИ, а не Apple-подписки. Юзер сказал "с кодами позже разберём" — но по факту это канал №1 по деньгам, приоритет высокий.**

---

## ✅ ДЕПЛОЙ СЕРВЕРА — ВЫПОЛНЕНО (26.06.2026)

Бэкенд развёрнут на том же VPS Contabo, где живёт мини-апп reader-bot, **строго изолированно** (отдельная БД/порт/поддомен, чужие сервисы не тронуты). Делали по шагам через SSH под `deploy` с Windows-ПК (ключ `beautycontent_cicd`).

⚠️ **Про мини-апп:** на VPS не только reader-bot — там же чужие сайты (`unibotz.com`, `beautycontentpro`) и процессы (`reader-bot-dev`, Qdrant, Redis). Это ОБЩИЙ продакшен-сервер, не «только reader-bot». Любая правка nginx — только своим файлом, перед reload всегда `nginx -t`. Юзер сказал: мини-апп reader-bot **отключим после публикации нового приложения** (пока работает рядом, не трогаем). Снапшот VPS юзер делать НЕ стал (его решение).

### Что сделано

| Шаг | Детали |
|-----|--------|
| **Домен** | `chitatel.app` куплен (Namecheap), ICANN-верификация email регистранта пройдена. A-записи `api` + `@` + `www` → `161.97.102.73` (TTL 5 мин). Корень/www направлены на наш сервер под будущий лендинг + Terms/Privacy (решено держать всё на `chitatel.app`). TXT SPF Namecheap оставлена. |
| **Node** | Системный Node на VPS — **v18** (под чужие проекты, НЕ трогать). Поставили **Node 20.20.2 через nvm локально под `deploy`** (`nvm install 20`, `nvm alias default 20`). Наш `engines: >=20` требует именно его. |
| **MongoDB** | Своя в Docker: контейнер **`chitatel-mongodb`**, образ `mongo:8.0`, порт **`127.0.0.1:27018`** (наружу не торчит), volume `chitatel-mongodb-data`/`-config`, `--auth`, root `chitatel_admin`. Изолирован от reader-bot (`reader-bot-mongodb-secure-clean` на 27017). Внутри контейнера Mongo слушает родной 27017 (для `docker exec` использовать 27017, снаружи — 27018). |
| **Бэкенд** | Клон `anna-busel/Chitatel_app` в `/home/deploy/chitatel/Chitatel_app`. `npm install` (545 пакетов, ок; warn'ы deprecated/audit игнорируем, `npm audit fix --force` НЕ запускать — сломает multer 1.x). |
| **.env** | `/home/deploy/chitatel/Chitatel_app/server/.env` (права 600, в .gitignore). Заполнено: PORT=3000, MONGO_URI (наш 27018), JWT_SECRET/JWT_REFRESH_SECRET/AUDIO_SECRET (сгенерены `openssl rand` на сервере), PUBLIC_BASE_URL=https://api.chitatel.app, APPLE_TEAM_ID=3GS6F87RKZ, APPLE_CLIENT_ID/BUNDLE_ID=app.chitatel.ios, ADMIN_EMAIL. **Пусто (по плану):** APPLE_KEY_ID/PRIVATE_KEY_PATH, GOOGLE_CLIENT_ID, OPENAI_API_KEY, APPLE_*_IAP, APNS_*. |
| **AUDIO_BASE_PATH** | Создан `/var/audio/chitatel` (owner `deploy`). Пустой — аудио зальём позже rsync'ом. |
| **PM2** | Глобальный PM2 на VPS стоит на системном Node 18 (обслуживает чужие процессы — НЕ трогать/обновлять). Наш процесс запущен через `ecosystem.config.js` с **`interpreter: /home/deploy/.nvm/versions/node/v20.20.2/bin/node`** (иначе пошёл бы на Node 18). Имя процесса **`chitatel-api`**, online, restarts 0, mode cluster (1 инстанс — норм). |
| **nginx** | Отдельный конфиг `/etc/nginx/sites-available/api.chitatel.app` (symlink в sites-enabled). proxy_pass → 127.0.0.1:3000, проброс WebSocket (Upgrade/Connection — для Socket.io чата), `client_max_body_size 12M` (картинки/голосовые), X-Forwarded-Proto. Чужие конфиги не тронуты. `nginx -t` чистый (чужой warn про `dev.unibotz.com` — не наш). |
| **HTTPS** | certbot 2.9.0 (уже был на сервере) → `sudo certbot --nginx -d api.chitatel.app`. Сертификат до 24.09.2026, авто-обновление настроено. HTTP→HTTPS редирект (301) работает. |
| **Каталог** | `npm run seed` прошёл: **54 платных + 3 бесплатных (57 книг) + 10 пакетов** в нашей Mongo. Маленький принц с 6 аудио-частями. API проверен: `https://api.chitatel.app/api/books` отдаёт реальные данные, `/api/health` → 200. |

### ⚠️ ХВОСТЫ деплоя (не блокеры, закрыть позже)

1. **Mongo-пароль засветился в логах** — `seed.js` печатает `MONGO_URI` целиком (строка `📡 Подключение к MongoDB: ...`) в открытом виде, пароль попал в чат. Риск низкий (Mongo на 127.0.0.1, наружу закрыт; сервер позже выводится из эксплуатации). **Рекомендация:** (а) сменить Mongo-пароль после деплоя (пересоздать контейнер + правка .env), (б) баг скрипта — замаскировать пароль в выводе `seed.js`. НЕ чинил без отмашки юзера.
2. **Заглушка «Нет описания»** — у 3 книг с пустым `description` в каталоге (Лолита, Тело помнит все, Пакет Латыпов+Франкл) `Book` требует description → seed падал. По решению юзера вписал `"description":"Нет описания"` прямо в `reader-bot-catalog.json` через GitHub (коммит `c8a2ccb`) — это правка ДАННЫХ, не логики; при получении реальных описаний от Анны перезапишутся. (Модель `Package` description НЕ требует — 4 пакета с пустым описанием прошли как есть.)
3. **Факультативы неполные** — seed выдал `⚠️` про 7 отсутствующих разборов (bednye_lyudi, dar, protsess, skazka_o_rybake_i_rybke, rusalochka, alenushka_ivanushka, skazka_o_mertvoy_tsarivne) + нет обложки `facultativ_tolstoy`. Известный хвост из каталога, Анна досылает. Не ошибки.
4. **Локальная правка на сервере?** — НЕ делали правок только-на-сервере (заглушку описаний внесли через репозиторий, поэтому `git pull` чист). `ecosystem.config.js` с `interpreter` — это локальная правка на сервере, в репо НЕ коммитили (путь к nvm-Node специфичен для этой машины). При будущем `git pull` файл не конфликтует (в репо его не меняли), но если когда-то поменяем `ecosystem.config.js` в репо — учесть локальную правку interpreter.

### Команды для повторного захода/обновления сервера
```bash
ssh -i <ключ> deploy@161.97.102.73
cd /home/deploy/chitatel/Chitatel_app && git pull origin main
cd server && npm install        # если менялись зависимости
pm2 restart chitatel-api        # после правок кода
pm2 logs chitatel-api --lines 20 --nostream
# seed заново (идемпотентно, чистит Book/Package):
npm run seed
# проверка снаружи:
curl -s https://api.chitatel.app/api/health
```

---

## КАТАЛОГ РАЗБОРОВ — ПЕРЕСОБРАН (14.06.2026)

Новый источник истины: `Каталог_разборов.xlsx`. Заменил прежний `reader-bot-catalog.json`.

**Итог:** **54 разбора + 10 пакетов** (4 тематических ПАКЕТА: `paket_woman`, `paket_love_rel`, `paket_goals_ach`, `paket_understand_yourself` + 6 ФАКУЛЬТАТИВОВ). Новые цены BYN+USD (USD под точки Apple `.99`). Слаги существующих книг и состав факультативов сохранены; #43-54 — транслит-слаги.

**Коммиты:** `seed.js` (`2c893cd`) · `reader-bot-catalog.json` (`2854ecf` → авторы `b31c037` → заглушки описаний `c8a2ccb`). Проверено: JSON валиден, цены 1-в-1.

**Авторы** проставлены #52 (Чехов и Бунин), #53 (Фромм), #54 (Холлис); 2 биографии — «еще не вышел» (автор не нужен). Юзер вручную переименовал `43.png` → `beguschaya_s_volkami.png`.

**Что осталось ДОСЛАТЬ** (отдано юзеру файлами `Каталог_что_дослать.md/.xlsx/.txt`, НЕ в репо):
- Обложки 11 разборов (#44-54) + `facultativ_tolstoy.png`.
- Описания: 3 (Лолита, Тело помнит все, Пакет Латыпов+Франкл) — **временно стоит заглушка «Нет описания»** (коммит `c8a2ccb`), при получении реальных перезаписать в каталоге + повторный seed.
- 7 разборов для факультативов (bednye_lyudi, dar, protsess, skazka_o_rybake_i_rybke, rusalochka, alenushka_ivanushka, skazka_o_mertvoy_tsarivne) — без них факультативы соберутся неполно.
- Аудио всех разборов (позже).

**PENDING:** «Пакет Латыпов + Франкл» (#51) — разбор или пакет? Обложки-серия в едином стиле — ОТЛОЖЕНО (рабочего ИИ-генератора в чате нет: Stability MCP падает).

**КАТАЛОГ → БАЗА (как заводится):** `seed.js` (`npm run seed`) читает `reader-bot-catalog.json`, чистит коллекции Book/Package, вставляет 54 платных + 3 бесплатных (Алиса/Ешь-молись-люби/Маленький принц с 6 реальными аудио) + 10 пакетов. Идемпотентный. Проставляет каждому `appleProductId = book.{slug}` / `package.{slug}`. Это заливка в НАШУ MongoDB — НЕ создаёт продукты в Apple. Цена priceUsd из каталога; bynToUsd — фоллбек. Обложки → `asset://book-covers/{slug}.png` (Flutter-ассеты). **Залито в прод-Mongo 26.06.2026.**

---

## ✅ ЗАДАЧА 3.1 — APP STORE CONNECT — СДЕЛАНО (16.06.2026, юзер в кабинете Анны)

Прошли вместе пошагово. Всё активно/создано:

**Юр.лицо / соглашения / анкеты:**
- Юр.лицо: **Hanna Busel**, ИП Тбилиси, адрес `Iv. Javakhishvili Street 91, Tbilisi 0198, Georgia` (исправляли — Apple ругался на кириллицу/дубли, заполнять латиницей по одному значению в поле). Идентификац./налоговый номер `94424749`.
- **Соглашение о платных приложениях (Paid Apps) — Активно.** ✅
- **Налоговые формы W-8BEN + Certificate of Foreign Status — Активно.** ✅
  - 6a Foreign Tax ID = грузинский налоговый номер. Пункт 5 (US TIN) — пусто. Part II (9 treaty + 10 special rates) — НЕ заявляли льготу (оставили пустым). ⚠️ Налоговое соглашение США–Грузия ЕСТЬ (через договор с СССР 1973, Грузия в списке IRS) — но льготу не заявляли для безопасности; при желании уточнить ставку у бухгалтера и переподать. Галочка "capacity to sign" обязательна для отправки.
- **Банк (Bank of Georgia, USD) — Активно.** ✅ Реквизиты (из PDF SOLO): Beneficiary **BUSEL HANNA**, IBAN **GE94BG0000000499009798**, SWIFT/«код банка в Грузии» **BAGAGE22**, валюта USD. «Номер счёта» = часть IBAN после кода банка (`0000000499009798`) либо полный IBAN — прошло (банк стал Активно). Адрес владельца = ИП-шный (не проверяется при платеже). Intermediary для USD: Citibank N.A. NY CITIUS33.

**Small Business Program (15% вместо 30%):**
- ✅ Заявка ПОДАНА (developer.apple.com/app-store/small-business-program → Enroll now; НЕ в разделе Business). Все associated-account вопросы = No (единственный независимый аккаунт). Ждёт письма Apple. ⚠️ Пока не одобрят — матрица цен показывает Proceeds по 30%; после одобрения пересчёт на 15%.

**Приложение:**
- Создано: **«Читатель: книжный клуб»**. **Apple ID приложения = 6779357856**, SKU `chitatel-ios-001`, Bundle ID `app.chitatel.ios`, язык русский.
- Категория, возрастной рейтинг, описание, скриншоты — НЕ заполнены (нужны для ПУБЛИКАЦИИ, не сейчас; не блокируют продукты/тесты). НЕ нажимать «Добавить для проверки» — это финальная отправка, рано (нет сборки).

**Продукты (subscription group «Клуб ЧИТАТЕЛЬ», group id 22166930):**
- ✅ **`club.basic.monthly`** — Apple ID продукта 6781739637, длительность 1 месяц, база США **$27.99**, локализация ru (назв. «Месяц» + описание). Статус «Метаданные отсутствуют» — НОРМА (не хватает скриншота для ревью, добьём в Фазе 7).
- ✅ **`club.basic.season`** — длительность **3 месяца**, база США ~$54.99, локализация ru. Создан.
- ⚠️ Цены по странам НЕ выровнены (в Грузии автопересчёт дал ~$35 из-за НДС 18% + округление; в США $27.99 «без налога»). Решили: шлифовать цены ПОТОМ; для важных Apple-стран (Грузия/Украина/Казахстан) переопределить вручную позже. Цена в Apple = «грязная» (минус НДС минус комиссия Apple); ориентир — колонка Proceeds, не витрина.
- ⚠️ Анна назвала цену **$28** — уточнить: это цена клиента или доход на руки (от этого зависит витринная цена; при 15% чтобы получить ~$28 на руки витрина ~$33).

**Отдельные разборы/пакеты в Apple (РЕШЕНИЕ юзера 16.06): продавать ЧЕРЕЗ Apple как non-consumable (разовые, доступ навсегда).** ID = `book.{slug}` / `package.{slug}` (seed их уже проставляет, бэкенд mapProduct уже понимает). 54+10 = 64 продукта. Заводить НЕ руками по одному, а СКРИПТОМ через App Store Connect API (POST /v2/inAppPurchases — официально поддерживает non-consumable). ⚠️ Честно: API капризный — продукт создаётся ок, но ЦЕНЫ отдельным эндпоинтом inAppPurchasePriceSchedules через price points (запутанно, бывают 500); СКРИНШОТЫ для ревью — обычно руками. Нужен ASC API key (Анна, роль Admin). Делать ПОЗЖЕ: сначала сервер → тест покупок на клубной подписке → потом скрипт на 64 продукта. НЕ сейчас.

**Осталось в кабинете (ПОЗЖЕ):**
- Sandbox-тестировщики (Users and Access → Sandbox) — для теста покупок.
- URL webhook (App Store Server Notifications) — **сервер теперь живой → можно прописать `https://api.chitatel.app/api/webhooks/apple`** (есть отдельный Sandbox URL для тестов).
- Категория приложения + возрастной рейтинг + метаданные — перед публикацией (Фаза 7).
- ASC API key (для скрипта продуктов-разборов И для верификации покупок на сервере — APPLE_*_IAP в .env).

**Тест покупок:** публикация в App Store НЕ нужна для теста. Тестируется через **Sandbox** (при разработке, деньги фейковые, подписки ускорены) и **TestFlight** (бета) — оба ДО публикации. Нужны: дописанное приложение + сервер + sandbox-аккаунты.

---

## 🔑 СОГЛАСОВАННАЯ МОДЕЛЬ ПОДПИСОК И ДОСТУПА (15.06.2026) — ВАЖНО

Долго и подробно проработана с юзером. Это финальное решение (цены могут двигаться).

### Приложение и оплата
- **Приложение БЕСПЛАТНОЕ на скачивание + встроенные покупки (IAP).** В сторе пометка «Бесплатно · Встроенные покупки». В App Store Connect цена приложения = Free; продукты IAP — со своими ценами. Платное-на-скачивании НЕ подходит.
- Всё, что покупается **в iOS-приложении** → ТОЛЬКО через Apple IAP (комиссия 15% Small Business / 30%). Никаких ссылок «оплати на сайте» внутри iOS (реджект 3.1.1). Цену в приложении закладывать выше, чтобы покрыть комиссию.

### Тарифы (subscription group «Клуб ЧИТАТЕЛЬ»)
- **Месяц** — `club.basic.monthly`, авто-подписка, ~$28. Основной тариф.
- **Сезон (3 месяца)** — `club.basic.season`, авто-подписка, ~$54. Apple-период «3 месяца» (штатный). Автопродление сохраняем (важно для удержания — не теряем людей между сезонами).
- **«Навсегда»** = `archive.forever`, non-consumable (разовая), не подписка. Позже.
- **Премиум** — позже, в ту же группу как `club.premium.*` (Apple сам upgrade/downgrade).
- ⚠️ **Сезоны НЕ делаем как отдельные продукты на каждый сезон и НЕ через non-renewing.** Один продукт «3 месяца» с автопродлением. «Лето/осень/зима» — это НАЗВАНИЕ и КОНТЕНТ, не отдельные Apple-продукты.

### Сезонность — как реализуется (всё на НАШЕМ сервере, не Apple)
- **Окно продажи сезона** — серверная логика: сезонный тариф показывается на paywall только в начале сезона (правило: первый месяц каждого сезона — лето=июнь, осень=сен, зима=дек, весна=мар). Вне окна — только месячный. Сервер сам открывает/закрывает по календарю, продукт в Apple — один, ничего «каждый раз прописывать» в Apple не надо.
- **Название сезонное** на paywall + честные условия рядом («Сезон · 3 месяца · продлевается автоматически») — Apple ОК.

### Правило доступа к контенту (НАШ сервер; Apple только «платит/не платит»)
- **Контент — помесячные клубы**, старт 1-го числа, ОБЩИЕ для всех активных (один чат, одна книга месяца). Сезон = единица ОПЛАТЫ, месяц = единица КОНТЕНТА (внутри сезона 3 месячных клуба подряд).
- **Доступ активного = скользящее окно «текущий месяц + предыдущий месяц»** (предыдущий = архив на **31 день** хвоста). Глубже прошлого месяца — закрыто (даже активному; если захотим «весь архив активным» — это другое правило, решить позже).
- **«+31 день» хвоста** — часть подписки, входит в неё. (Было 21 на сайте → решили 31. См. правку кода ниже.)
- **Деньги — по личной дате Apple** (у каждого своя дата продления, от покупки). **Контент — по календарю** (наш сервер, общий для всех). Они НЕ совпадают по датам — это норма. Доступ привязываем к календарным клубам месяца, НЕ к «30 дням от покупки».
- Купил в середине → не обделён: Apple даёт полный оплаченный период от даты покупки + 31 день хвоста; в общий контент попадает сразу.
- Отписался → Apple держит до конца оплаченного периода, последний месяц ещё 31 день, дальше нет.

### Что НЕЛЬЗЯ через Apple (остаётся сайту)
- **Предпродажа заранее** (продать летний сезон весной): авто-подписка стартует и списывает В МОМЕНТ покупки, отложить старт на 1-е число нельзя. Окно продажи сезона = в начале самого сезона, не раньше. Предоплата «за лето весной» — ТОЛЬКО сайт.
- Рассрочка, подарочные сертификаты, оплата российскими/любыми картами — только сайт.

### Анонсы и пуши
- **Анонс будущего клуба/сезона МОЖНО заранее** (это наш контент, Apple не регулирует). В модели уже есть статус `future` (API клуба: `{archive, current, future}`).
- Граница: анонсировать заранее — да; продавать/списывать за будущий период через Apple — нет (только со старта). На анонсе допустима кнопка «Напомнить»/«Записаться» (это НЕ оплата).
- **Анонс нового клуба месяца → broadcast-пуш ВСЕМ** (регулярное событие). «Напомнить» — для разовых/сезонных запусков. Слать только тем, кто разрешил уведомления (настройки в профиле `profilePushSettings` уже есть). Реализация пушей — **Фаза 6**.

---

## 🧩 БУДУЩАЯ ФИЧА: КОДЫ АКТИВАЦИИ (Россия/Беларусь + предпродажа + штучные продажи) — ПРИОРИТЕТ ВЫСОКИЙ

⚠️ Не v1 по коду, но это КАНАЛ №1 ПО ДЕНЬГАМ (основная аудитория РФ+РБ не может платить через Apple). Требует поднятого сервера — **сервер теперь есть (26.06)**, можно браться.

- **Зачем:** (1) Россия/Беларусь — Apple-оплата для их карт НЕ работает, продаём на сайте; (2) предпродажа сезона заранее; (3) отдельные разборы/пакеты вне Apple.
- **Как:** человек платит на сайте (карты, рассрочка) → получает **код** → вводит в приложении в нейтральном поле «Активировать код» → наш сервер открывает доступ (разбор/пакет/сезон). Можно для ЛЮБОГО разбора и клуба (ложится на product-agnostic `Purchase`).
- **Правило Apple (жёстко):** в приложении ТОЛЬКО нейтральное поле «активировать код», БЕЗ рекламы покупки на сайте, без кнопок/намёков «купи на сайте», БЕЗ гео-показа «россиянам — на сайт». Поле одинаковое для всех. Продажа и реклама — снаружи (рассылки, Telegram, сайт). Так живут Kindle/Spotify — легально.
- **Код = не Apple-подписка → без автопродления** (для росс./бел. карт это и так неизбежно). Следующий сезон — покупают заново.
- **Объём:** новая модель `ActivationCode` (код → товар → срок → использован/кем) + экран активации + связка с оплатой сайта. Связка: сначала ВРУЧНУЮ (менеджер выдаёт код после оплаты), потом авто-интеграция платёжки сайта с нашим API.
- **Юзер сказал "с кодами позже разберём"** — зафиксировано, но напомнить, что это основной денежный канал.

---

## ЗАДАЧА 3.3 — БЭКЕНД-ВЕРИФИКАЦИЯ — ✅ ГОТОВО (14.06)

- `models/Purchase.js` (`20f8d00`) — product-agnostic; `transactionId`(=originalTransactionId) unique sparse; itemType book/package/subscription/archive.
- `routes/purchases.js` (`3821edb`) — `POST /api/purchases/verify` (requireAuth, Zod `{signedTransaction}`).
- `services/purchase.service.js` (`889b87c` → рефактор `6462df5`) — экспортит `{verifyPurchase, applyTransaction, getVerifier, mapProduct}`. `applyTransaction({userId, decodedTransaction, statusOverride})` — upsert Purchase + права User; statusOverride 'refunded'/'expired'/'cancelled' для webhook (revoke снимает права). `mapProduct`: archive.forever→archive; club.{tier}.{period}→subscription; book.{slug}/package.{slug}→разовые (lookup по slug).
- `app.js` (`31ea07a`), `config/index.js` (`f1e4514` — apple.bundleId/issuerId/appAppleId/environment/rootCertsPath), `package.json` (`7839dc5` — +@apple/app-store-server-library ^3.1.0).
- `User.js` НЕ менялся (поля подписки уже были).
- ⚠️ `subscriptionPlan` enum = `['monthly','semiannual','annual']`, 'season' НЕ пишем (период в `Purchase.itemId`). Когда финализируем — **добавить 'season' в enum User**.
- ⚠️ **Для работы верификации в проде нужны APPLE_*_IAP в .env (ASC API key + rootCerts) — пока пусто.**

## ЗАДАЧА 3.4 — S2S WEBHOOK — ✅ ГОТОВО (14.06)

- `services/webhook.service.js` (`7045ae1`) — `handleNotification(signedPayload)`: verifyAndDecodeNotification → verifyAndDecodeTransaction → найти Purchase по originalTransactionId → applyTransaction. REVOKE_TYPES{REFUND,REVOKE}→'refunded'; EXPIRE_TYPES{EXPIRED,GRACE_PERIOD_EXPIRED}→'expired'; renew/subscribe — по expiresDate. Purchase не найден → лог+ack.
- `routes/webhooks.js` (`981bc75`) — `POST /api/webhooks/apple`, БЕЗ авторизации, Zod `{signedPayload}`, ВСЕГДА 200 (try/catch+log).
- `app.js` (`9f766d7`) — `+/api/webhooks`.
- ⚠️ **URL для App Store Connect: `https://api.chitatel.app/api/webhooks/apple` — сервер живой, можно прописывать (Sandbox URL отдельно).**

## ЗАДАЧА 3.2 — FLUTTER PAYWALL — ✅ ГОТОВО (15.06)

- `core/network/api_endpoints.dart` (`154fc7d`) — `+purchasesVerify`.
- `features/payments/services/purchase_service.dart` (`41a3c84`) — StoreKit 2 через `in_app_purchase`, общий (любой productId): isAvailable, queryProductDetails, purchaseStream, buyNonConsumable, restorePurchases, completePurchase, verifyOnServer(jws). Продукты: `club.basic.monthly`, `club.basic.season`.
- `features/payments/providers/purchase_provider.dart` (`f7431d9`) — Riverpod, статус PaywallStatus (не путать с PurchaseStatus из пакета), слушает purchaseStream → на purchased/restored шлёт `serverVerificationData` (JWS) на /verify → success.
- `features/payments/screens/success_screen.dart` (`cf1f287`) — 4.29.
- `features/payments/screens/paywall_screen.dart` (`e75bad4`) — 4.28: тарифы (цена из App Store), юр.текст автопродление/как отменить, ссылки Terms/Privacy, кнопка Restore.

**⚠️ Ручные шаги юзера для 3.2 (код их требует, я не могу) — ЕЩЁ НЕ СДЕЛАНЫ:**
1. `cd app && flutter pub add in_app_purchase url_launcher` (НЕ пинить версии руками — резолвер подберёт под Flutter 3.22.3; урок #27). Потом `cd ios && pod install`.
2. Роутер `app/lib/core/router/app_router.dart`: добавить `import '../../features/payments/screens/paywall_screen.dart';` и заменить `const _Placeholder('Тарифы')` на `const PaywallScreen()` (сейчас там заглушка; весь 316-строчный файл не переписывал ради 2 строк).
3. Xcode: capability **In-App Purchase** на Runner.
4. Ссылки Terms/Privacy в paywall — заглушки `chitatel.app/terms|privacy`. Заменить на рабочие. **РЕШЕНО (26.06): держать лендинг + Terms/Privacy на `chitatel.app`** (корень домена направлен на наш VPS). Создать страницы после деплоя — отдельная задача.
5. Product ID в коде = `club.basic.monthly`/`club.basic.season` — Анна создала продукты с ЭТИМИ ID ✅ (3.1 готова).

**⚠️ Тест:** Flutter SDK в окружении Claude нет → проверено только соответствие паттернам/API. Реальный тест — sandbox после ручных шагов (сервер теперь есть).

## ⚙️ Правка: архивный доступ 21 → 31 день

Согласовано «+31 день» хвоста. `models/ClubMonth.js` (`d2370d1`) — комментарий правила обновлён на 31. ⚠️ Функциональное число `+21` ОСТАЛОСЬ в `scripts/seed-club.js` (`upsertClubMonth`: `getDate() + 21`) — юзеру поменять на `+ 31` (тестовый seed, не стал переписывать 19KB ради строки). Полноценная логика доступа (скользящее окно, сезоны, окна продаж) В КОДЕ ЕЩЁ НЕ НАПИСАНА — будущая задача по клубу; при её написании зашить 31 как именованную константу.

---

## ДАЛЬШЕ ПО ПЛАНУ

```
✅ ДЕПЛОЙ СЕРВЕРА ВЫПОЛНЕН (26.06) — api.chitatel.app по HTTPS, своя Mongo, каталог залит
Фаза 3 (Платежи) — КОД ГОТОВ (3.3 ✅ 3.4 ✅ 3.2 ✅) + КАБИНЕТ 3.1 ✅
СЛЕДУЮЩЕЕ (по важности для денег):
  1. seed:club на сервере (тестовые клубы) — для теста чата/клуба в проде (НЕ делали в этой сессии).
  2. Ручные шаги 3.2 (pub add/роутер/Xcode capability/ссылки) — на маке/в приложении.
  3. ASC API key от Анны → вписать APPLE_*_IAP в .env (верификация покупок) + webhook URL в ASC + sandbox-аккаунты.
  4. Тест покупок: свой физ.айфон по кабелю (без Codemagic) ИЛИ Codemagic+TestFlight для айфона Анны (удалённо).
  5. Лендинг + Terms/Privacy на chitatel.app (корень уже направлен на VPS).
  6. Коды активации (Россия/Беларусь — КАНАЛ №1) — сервер есть, можно браться.
  7. Логика доступа к клубу (скользящее окно + 31 день + сезоны + окна продаж) — НОВАЯ задача по клубу.
  8. Продукты-разборы в Apple (64 non-consumable book.*/package.* скриптом через ASC API).
  9. Фаза 5 (ИИ-дневник), Фаза 6 (полировка + ПУШИ), Фаза 7 (TestFlight через Codemagic).
```

**От Анны нужно:** ASC API key (для APPLE_*_IAP верификации + продуктов-разборов); уточнить $28 — цена клиента или доход; наполнение Базовый (и потом Премиум). Прочие вопросы — см. AC-2.

**Тест-аккаунты (seed:club):** anna@chitatel.app/anna123456 (admin), test-premium@chitatel.app/test123456 (premium активный), test-basic, test-expired. ⚠️ На сервере seed:club ещё НЕ запускали — для теста клуба в проде нужно `npm run seed:club`.

---

## УРОКИ (#28+; #1-27 в AC/AC-2)

**#28 — формат/версию пакета проверять по registry перед использованием.** `@apple/app-store-server-library` 3.1.0, CJS (main dist/index.js, без type:module) → require ок. API сверен по докам, не по памяти. Продолжение #21/#22/#27.

**#29 ⚠️ Apple-подписка: старт = момент покупки, период = от даты покупки (15.06).** Нельзя отложить старт (предпродажа «за лето весной» через Apple невозможна → сайт). Нельзя «календарный сезон для всех» на авто-подписке (у каждого своя дата). Решение: деньги по дате Apple / контент по календарю (наш сервер); сезонность = окно продаж + название + контент, НЕ отдельные продукты и НЕ non-renewing.

**#30 — активация контента, купленного вне приложения, легальна (Kindle/Spotify-модель), НО только нейтральное поле «активировать код», без рекламы внешней покупки в приложении (15.06).**

**#31 ⚠️ ГЛАВНОЕ СТРАТЕГИЧЕСКОЕ (16.06): Apple-оплата НЕ работает в РФ и Беларуси (карты не принимаются с 2022).** Основная аудитория проекта — РФ+РБ+Грузия. Через Apple платит только Грузия/Украина/Казахстан/заграница. Значит Apple-подписки покрывают МЕНЬШУЮ часть аудитории; основной денежный канал — САЙТ + КОДЫ АКТИВАЦИИ. Цены в Apple для РФ/РБ бессмысленно выравнивать (там не оплатят). Не выравнивать цены по 175 странам — это тупик из-за разных НДС/округлений; единая честная цена даётся на сайте.

**#32 — ценообразование Apple (16.06):** витрина = НДС страны + комиссия Apple + твои поступления (Proceeds). Цены берутся из фикс. «ценовых точек» (не любое число; $27.99 для подписок может не быть → $28.99). США показывает цену БЕЗ налога (налог штата сверху у покупателя), многие страны (Грузия и т.п.) — С НДС внутри + округление вверх → витрина выглядит дороже, но Proceeds сопоставимы. Ориентир — колонка Proceeds, не витрина. Базовую страну ставить США/USD (не Грузию — иначе всё едет вверх). Отдельные страны переопределять вручную точечно. До одобрения Small Business матрица считает Proceeds по 30%, после — 15%.

**#33 — продукты-разборы через ASC API (16.06):** создание non-consumable через POST /v2/inAppPurchases официально поддерживается, но API капризный: цены отдельным эндпоинтом inAppPurchasePriceSchedules через price points (бывают 500), скриншоты обычно руками. Не «за 5 минут» — отдельная инженерная задача. Сначала проверить покупки на 1-2 продуктах, потом массово. ID навсегда — не ошибаться в Product ID при массовом прогоне.

**#34 ⚠️ ПРОЦЕСС: при обновлении AI-CONTEXT через GitHub MCP ВСЕГДА отдавать ПОЛНЫЙ файл (сначала get_file_contents → дописать → запушить целиком), НИКОГДА не слать только новый кусок (16.06).** Один раз случайно затёр AC-3 (30KB→1.6KB) одним абзацем; восстановил из истории git (коммит `265f8b9`). create_or_update_file ПЕРЕЗАПИСЫВАЕТ весь файл, а не дополняет.

**#35 ⚠️ ОБЩИЙ ПРОДАКШЕН-СЕРВЕР — ИЗОЛЯЦИЯ ОБЯЗАТЕЛЬНА (26.06).** VPS оказался не «только reader-bot»: там чужие сайты (unibotz.com, beautycontentpro), процессы (reader-bot-dev, Qdrant, Redis), системный Node 18, глобальный PM2 на чужих процессах. Деплой нашего — строго изолированно: Node 20 через nvm локально под `deploy` (не systemwide), своя Mongo в отдельном контейнере на отдельном порту 27018, свой PM2-процесс с `interpreter` на Node 20, свой nginx-конфиг отдельным файлом. Перед каждым `nginx reload` — `nginx -t`. Чужое не трогать/не обновлять (особенно глобальный PM2 и системный Node). Разведка перед действиями (ss/docker ps/pm2 status/nginx -t) — обязательна.

**#36 ⚠️ СЕКРЕТЫ В ЛОГАХ СКРИПТОВ (26.06).** `seed.js` печатает `MONGO_URI` целиком (с паролем) в открытом виде → пароль засветился в чате. При деплое следить, что выводят скрипты; секреты в логах маскировать. Mongo-пароль после деплоя сменить (был на 127.0.0.1, риск низкий, но гигиена). Юзеру: вводить пароли в приглашении (mongosh -u ... затем prompt), не в командной строке/чате. Заглушки/правки данных каталога — через репозиторий (видно в истории), а не молча на сервере (урок про источник истины).

---

*Обновлён 26.06.2026. **✅ ДЕПЛОЙ СЕРВЕРА ВЫПОЛНЕН:** `https://api.chitatel.app` работает по HTTPS (Node 20 через nvm под deploy, своя Mongo в Docker на 27018, PM2 chitatel-api, nginx+certbot, изолировано от reader-bot на общем VPS). Каталог залит (54+3+10). Домен chitatel.app: ICANN-верификация пройдена, A-записи api/@/www. Хвосты: Mongo-пароль засветился в логах seed (сменить + замаскировать), заглушки «Нет описания» у 3 книг (коммит c8a2ccb), факультативы неполные (Анна досылает), .env неполон (APPLE_*_IAP/OPENAI/APNS пусто). Уроки #35 (изоляция на общем сервере) #36 (секреты в логах). Дальше: seed:club на сервере → ручные шаги 3.2 → ASC API key + webhook URL + sandbox → тест покупок (физ.айфон/Codemagic) → лендинг+Terms/Privacy → коды активации. Прогресс фиксировать далее ТОЛЬКО здесь.*
