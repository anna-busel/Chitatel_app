# AI-CONTEXT-5

Продолжение `AI-CONTEXT-4.md` (тот закрыт на разделе 10, 15.08.2026).
Читать по порядку: `AI-CONTEXT.md` → `-2` → `-3` → `-4` → **этот файл**.
Полный список находок аудита и чеклист подачи — `docs/AUDIT-2026-08-PRESUBMIT.md`.

---

## 11. Аудит перед сабмитом и все фиксы (16.08.2026, всё в `main`)

### 11.1 Что произошло

По замороженному коду проведён независимый аудит **другой моделью** (fable 5,
свежий контекст, код писала другая модель) по пяти направлениям: правила
Apple, безопасность, устойчивость при росте, функциональная корректность,
операционная готовность. Находки перепроверены вручную — ложных не было.

**Все кодовые находки исправлены и залиты** — 43 файла, каждый сверен
побайтово по blob SHA против `origin/main`. `node --check` на всех 80 серверных
файлах — чисто; экспорты/импорты между изменёнными модулями согласованы.

### 11.2 Главное из исправленного

Полная таблица — в `docs/AUDIT-2026-08-PRESUBMIT.md`. Кратко, что важно помнить:

**Платежи (самое критичное).** Была реальная мина: верификация чеков шла
против ОДНОГО окружения. Apple тестирует покупки **в sandbox даже на
продакшен-билде** — с `APPLE_ENVIRONMENT=production` все покупки ревьюера
отвалились бы и это гарантированный реджект. Теперь `purchase.service` держит
два верификатора и ретраит в другом окружении на статусе 5
(INVALID_ENVIRONMENT). Кроме того: `complete()` только после успешного verify
(иначе оплаченная покупка терялась), refund снимает доступ, чужую транзакцию
нельзя привязать к своему аккаунту, вебхук отвечает 500 при внутренней
ошибке (Apple ретраит), «Восстановить покупки» больше не вешает спиннер.

**Ключи месяцев клуба.** Формат был несогласован: местами `2026-07`,
местами `2026-7`. Единый формат — **без ведущего нуля** (`2026-7`).
Для уже записанных в БД ключей есть разовый скрипт:
`node src/scripts/fix-club-month-keys.js` — **запустить один раз при деплое**.

**Новая общая функция доступа.** `computeClubAccess(user, club, now)` в
`middleware/subscription.js` — чистая, без `req`. Её теперь используют
`resolveClubAccess` (HTTP), `socket/index.js` и `assertClubAccessForMessage()` в
`routes/club.js`. Если меняешь правила доступа — меняй только её.

**Клиент.** Убраны обе dev-заглушки с карточки разбора (отзывы «появятся
позже» и кнопка «Пожаловаться» с текстом «в задаче 4.5») — это был почти
гарантированный реджект по Guideline 2.1. Ссылка «Забыли пароль?» скрыта
(серверного эндпоинта сброса нет). Экраны, открытые из пуша, теперь всегда
имеют выход (было нарушение Guideline 4.0).

**Масштаб.** Push-рассылка чанками по 100 в фоне; ИИ-отчёты батчами по 3
с защитой от параллельного прогона; аудио грузится на диск, а не в память;
`max_memory_restart` поднят до 900M.

### 11.3 Деплой (на владельце)

```
git pull
npm install
node src/scripts/fix-club-month-keys.js   # разово
pm2 restart chitatel-api
```

Перед рестартом проверить `.env`: `JWT_SECRET`, `JWT_REFRESH_SECRET`,
`AUDIO_SECRET` — не дефолтные и от 32 символов, иначе сервер теперь
**намеренно не стартует** в production (S9). Клиентские правки подтянутся
только новой сборкой.

### 11.4 Что осталось до подачи

Кодом больше ничего не требуется. Осталось четыре блока, все на владельце:

1. **`.env` и деплой** — `APPLE_ENVIRONMENT=production`, секреты, `APPLE_KEY_ID` +
   `APPLE_PRIVATE_KEY_PATH` (ключ Sign in with Apple), почта `support@chitatel.app`,
   смена пароля админа (`anna123456`).
2. **Сборка TestFlight (Фазы 5+6 вместе) и прогон 6.7** на устройстве:
   Dynamic Type на максимум, VoiceOver, оффлайн, фоновое аудио,
   sandbox-покупка и продление (DID_RENEW ещё не гоняли).
3. **App Store Connect** — IAP в Ready to Submit и привязаны к билду,
   Privacy labels (указать передачу цитат в OpenAI), демо-аккаунт (6.8),
   Review Notes.
4. **Контент клуба к ревью** — живой чат, пара отвеченных Q&A, желательно
   один архивный клуб. ⚠️ `seed-club.js` на проде НЕ запускать — затрёт чат.

Детали по каждому пункту — в `docs/AUDIT-2026-08-PRESUBMIT.md`, раздел 2.

### 11.5 Уроки этой сессии

- **Урок #20.** Аудит чужой моделью окупился: июльский аудит пропустил две
  dev-заглушки в `book_screen.dart`, потому что файл глубоко не читался
  (это было прямо отмечено в его «слепых зонах»). Перед сабмитом аудит
  — не формальность, а инструмент, который говорит, готов ли ты.
- **Урок #21.** При заливке пачки файлов через GitHub API важен **порядок**:
  файл, который импортирует новый символ, нельзя коммитить раньше того, где
  символ объявлен — иначе `main` на минуту нерабочий.
- **Урок #22.** Побайтовая сверка после каждого пуша (`git hash-object`
  против `git rev-parse origin/main:<file>`) ловит тихие повреждения текста
  — в этой сессии так нашлась сломанная буква в комментарии.

---

## 12. Правка модели доступа к архиву + наполнение чата (17.08.2026)

### 12.1 Найденный баг (деньги)

Архивный доступ **не сверялся с оплаченным набором `clubMonthsEntitled`**:
- `middleware/subscription.js`, `computeClubAccess` — архивная ветка пускала по `everSubscribed`, то есть любого, кто когда-либо платил;
- `routes/books.js`, `userHasBookAccess` — архивная ветка возвращала `true` любому активному подписчику.

Следствие: человек, впервые оплативший сентябрь, бесплатно получал **весь августовский клуб** — чат, Q&A и платный разбор. Это тот же класс бага, что чинили 30.07 для текущего клуба, только в обратную сторону.

### 12.2 Согласованная модель (владелец, 17.08.2026)

Оплатил месяц → этот клуб активен весь свой календарный месяц; **следующий календарный месяц он остаётся полностью рабочим как архив** (чат, Q&A, разбор) — это бонус-месяц. Продление на бонус не влияет: `expired` тоже дослушивает и дообсуждает свой оплаченный клуб. Клуб, которого нет в `clubMonthsEntitled`, **не открывается никогда** — ни как текущий, ни как архивный.

### 12.3 Что изменено

- `server/src/middleware/subscription.js` — архивная ветка `computeClubAccess`: `everSubscribed` → `isInArchiveWindow && isPaidClub`. Активная подписка не требуется. Обновлены доккоменты (правило 5 и абзац про архив).
- `server/src/routes/books.js` — `userHasBookAccess`: убран ранний `if (!hasActiveSub) return false` (он отбирал у `expired` его же оплаченный разбор); добавлена сверка `coveredKeys.has(clubMonthKey(club))` для ВСЕХ веток; текущий клуб теперь требует `hasActiveSub`, архивный — нет.
- `server/src/routes/club.js` — список клубов: архивные документы фильтруются по `clubMonthsEntitled` (не-админам). Раньше в дропдауне висел клуб, который человек не оплачивал.
- Правки помечены в коде маркером `⛔️ 17.08.2026` с описанием бага, чтобы их не откатили обратно.
- Чат, Q&A и socket ходят через общий `computeClubAccess`, поэтому одна правка закрыла все три канала.

### 12.4 Последствие для ревью (важно)

Аккаунту ревьюера подписка выдаётся из админки, и она кладёт в `clubMonthsEntitled` месяцы указанного периода. Если выдать «с сегодня» (август), ревьюер **не увидит архивный клуб** («Гроздья гнева», июль) — его просто не будет в списке. Чтобы архив был виден, дату начала подписки надо ставить **с июля**.

### 12.5 Наполнение чата клуба (`scripts/seed-club-chat.js`)

Добавлен `server/src/scripts/seed-club-chat.js` — пишет первые сообщения напрямую в базу с разбросом дат по дням. С телефона так нельзя: сервер всегда ставит `createdAt = now`, и два десятка сообщений подряд выглядят как постановка.

- Флаги: `--dry-run`, `--anna=EMAIL`, `--ksenia=EMAIL`, `--no-rename`, `--clean`.
- Пишет сырым драйвером `ChatMessage.collection.insertMany`, иначе mongoose с `timestamps: true` перезатирает `createdAt`.
- Идемпотентен по паре (автор, текст) среди живых сообщений; `--clean` удаляет мягко, через `deletedAt`.
- Состав: 14 сообщений в текущем клубе («Идиот») + 6 в архиве («Гроздья гнева»). Три участницы — переименованные тестовые аккаунты (test-premium → «Марина Л.», test-basic → «Оля», test-expired → «Дарья») плюс Анна с её настоящего админского аккаунта `misswoolly@yandex.ru`: плашка «ведущая» считается живьём из `role === 'admin'`, поэтому её сообщения должны идти именно с него.
- Голосовое скриптом не кладётся — его записывает только человек в приложении и только админ.

**Это НЕ `seed-club.js`:** тот пересоздаёт клубы и безвозвратно вытирает чат, на проде его запускать нельзя.

### 12.6 Зачем чат нужен для ревью

Guideline 1.2 (UGC): ревьюер должен найти жалобу на контент и блокировку пользователя, а обе кнопки появляются только на **чужом** сообщении. Пустой чат = кнопок не видно = риск отказа.

### 12.7 Деплой

Все правки серверные, пересборка Flutter **не нужна**: `git pull` + `pm2 restart chitatel-api`.

### 12.8 Коммиты (main), все побайтово сверены

`f9630bf` seed-club-chat.js (28309 б); `5826984` subscription.js (19763 б); `fd8e9dc` books.js (18199 б); `a64d79e` club.js — фильтр архива в списке (71147 б); `0cbbe7a` club.js — битый символ в комментарии про mime-whitelist, `по白списку` → «по белому списку» (71158 б).

### 12.9 Скан репозитория на битые символы

Прогнан скан всего репозитория на посторонние кодепоинты (следы порчи символов при заливках через API). В `server/src` и `app/lib` чисто. Остался один безобидный `＋` (U+FF0B, fullwidth plus) в `server/admin/index.html` — это глиф кнопки, рендерится нормально, не трогали.

---

## 13. Подготовка к подаче: пейвол, аккаунты ревьюера, иконка (18.08.2026)

### 13.1 Правка клиента: крестик на пейволе

`PaywallScreen` получил параметр `showClose` (по умолчанию `true`).

Было: крестик рисовался всегда и звал `Navigator.maybePop()`. В четырёх точках
входа пейвол открыт маршрутом (`home_screen.dart:95`, `club_screen.dart:165` и
`:176`, `app_router.dart:252`) — там он работает. Но в табе «Клуб» без подписки
пейвол подставляется как обычный виджет внутрь `ShellRoute`
(`club_screen.dart:83`), маршрута нет, `canPop()` = false, и кнопка молча
ничего не делала. Это ровно тот путь, который увидит рецензент.

Стало: в табе передаётся `showClose: false`, крестика там нет, выход нижней
навигацией. Остальные вызовы не тронуты — параметр необязательный.

Коммиты: `f943e81` paywall_screen.dart (15256 б), `700f003` club_screen.dart
(16697 б). Оба сверены побайтово. ⚠️ `dart analyze` не прогонялся — в песочнице
нет Flutter, компиляция проверится при сборке.

### 13.2 Аккаунты для App Review — созданы и проверены

Два аккаунта, оба зарегистрированы в приложении, онбординг пройден до конца
(иначе рецензента разворачивает на обязательный экран «Имя»), ИИ-анализ включён.

**Логины и пароли в репозитории НЕ хранятся** — они в проектном документе
`claude/reviewer-accounts.md` (Claude Project «Chitatel.app»). Причина: на
18.08 репозиторий ещё публичный.

- **Аккаунт 1** — без подписки, идёт в поля Demo Account. Рецензент упирается
  в пейвол и покупает месяц в sandbox. Именно это Apple и проверяет.
- **Аккаунт 2** — `basic` / `season` до 18.11.2026, набор месяцев
  `2026-7, 2026-8, 2026-9, 2026-10`. Июль добавлен вручную через
  `$addToSet` — админка его выдать не может, `clubMonthKeysBetween` считает
  только от сегодня вперёд. Июль нужен ради архивного клуба «Гроздья гнева».
  Указывается в Notes как запасной.

Зачем второй: sandbox-подписка продлевается раз в несколько минут и умирает
после шести продлений, примерно через полчаса. Без запасного аккаунта рецензент
после паузы снова упрётся в пейвол на «оплаченном» аккаунте.

Проверено на устройстве: клуб открывается, чат с сообщениями виден, архив
доступен.

**Пароли только из строчных букв и цифр** — см. 13.4.

### 13.3 basic и premium не отличаются ничем

Проверено по всему коду: везде `'basic' || 'premium'` либо
`$in: ['basic','premium']` — доступ к клубу (`subscription.js:248`), к аудио
(`books.js:386`), пуши, аналитика. Ни одной строки, где premium давал бы
больше. Продукта `club.premium.*` не существует, это задел на будущее
(`purchase_service.dart:27`). Плана `annual` среди покупаемых тоже нет.
Реальные продукты: `club.basic.monthly`, `club.basic.season`.

Следствие: при ручной выдаче из админки брать `basic` и `season` —
`_planTitle` в `manage_sub_screen.dart:123-132` отдаёт «Месяц» для любого
плана кроме `season`, поэтому `annual` показал бы бессмыслицу.

### 13.4 Отложенный дефект: шифт на экранах входа и регистрации

Симптом: на экранах входа и регистрации заглавная буква вводится через раз.
В чате клуба и в цитатах — работает нормально.

Различие полей:

| где | autocorrect / enableSuggestions | textCapitalization | шифт |
|---|---|---|---|
| чат (голый `TextField`) | true (дефолт) | none | ок |
| цитата → «Автор» | false | words | ок |
| регистрация → «Имя» | false | none | глючит |
| пароли | false + `obscureText` | none | глючит |

Ломается на сочетании «подсказки выключены + капитализация none». Причина
НЕ подтверждена — воспроизвести без устройства нельзя, а «через раз» плохо
вяжется со статичной конфигурацией (скорее гонка при перестройке клавиатуры,
например когда всплывает панель автозаполнения паролей).

Решение отложено осознанно: править вслепую перед сборкой, которую подаём,
значит добавить непроверенный диф и не понять, помогло ли. На ревью не влияет —
пароли демо-аккаунтов набираются без единого шифта.

Что делать при прогоне 6.7: зарегистрировать аккаунт с паролем, где есть
заглавные, посчитать промахи из десяти и заметить, привязаны ли они к появлению
панели «Пароли». С этими данными чинится прицельно, в первом обновлении.

Кандидаты на правку, когда дойдут руки:
- `email_register_screen.dart:113` — поле «Имя» → `TextCapitalization.words`
- поля пароля — снять лишние `autocorrect: false, enableSuggestions: false`
  (при `obscureText` iOS их и так игнорирует)
- заодно шесть полей клуба без капитализации, где нужна `sentences`:
  `chat_input.dart:386`, `ask_question_sheet.dart:146`, `qa_tab.dart:119`,
  `chat_tab.dart:694` и `:1056`

### 13.5 Иконка приложения

Прислана монограмма BB, кремовая на тёмно-красном. Проверено: 1024×1024, RGB,
альфа-канала нет, углы прямые — формальным требованиям Apple соответствует.
Читается вплоть до 58 px (Настройки).

Заливается ТОЛЬКО в проект: `app/ios/Runner/Assets.xcassets/AppIcon.appiconset/`,
все 15 файлов классического набора Flutter. В App Store Connect поля для
загрузки иконки у iOS-приложений нет — она берётся из сборки. После замены
обязателен `flutter clean`, иначе старая залипает в кэше.

Замечание не по ревью: монограмма латинская, приложение называется «ЧИТАТЕЛЬ»
и ищется по-русски. Связи между иконкой и названием нет — на одобрение не
влияет, на узнаваемость в поиске влияет.

### 13.6 App Store Connect

Заполнена «Информация для проверки приложения»: требуется вход, демо-аккаунт,
контактная информация, англоязычные Notes с описанием гостевого режима,
покупок, модерации по Guideline 1.2 и удаления аккаунта. Текст Notes целиком —
в `claude/reviewer-accounts.md`.

Известная проблема: страница «Подготовка к отправке» периодически падает с
«невозможно обработать запрос». Диагностика не завершена. Проверять по порядку:
другой браузер (Safari у ASC исторически проблемный), английский язык
интерфейса, состояние Paid Applications agreement, не обрабатывается ли ещё
сборка.

### 13.7 Что осталось до подачи

Ничего из этого не требует кода:

- ротация утёкших ключей (OpenAI, Mongo, JWT×2, AUDIO_SECRET), смена пароля
  сидового админа `anna@chitatel.app`, перевод репозитория в приватный
- `APPLE_ENVIRONMENT=production`, Node ≥ 20
- каждой покупке — скриншот для проверки и заметки, иначе IAP не уйдут на ревью
- скриншоты 6.7″ и 6.1″, описание, ключевые слова, Age Rating
- Privacy labels с **User Content**
- прогон 6.7 на устройстве
- **сентябрьский `ClubMonth` до 1 сентября** — иначе `GET /api/club/current`
  отдаст 404, а клиент ловит только `SUBSCRIPTION_REQUIRED` и уйдёт в
  бесконечный Retry (`club_screen.dart:83-97`). Ударит по всем, не только по ревью

### 13.8 Уроки

- **Урок #23.** Перед тем как дописывать в `AI-CONTEXT`, проверять, какой файл
  сейчас открыт. Раздел 11 был дописан в закрытый `-4`, хотя актуален уже
  `-5` с собственным разделом 11. Исправлено переносом (`764b49f`, `d7d788d`).
- **Урок #24.** `push_files` (git trees API) может стабильно отдавать 500,
  когда `create_or_update_file` (contents API) работает. Это разные эндпоинты —
  при отказе одного пробовать другой, а не ждать.
- **Урок #25.** Крупные файлы (70 КБ и больше) заливать отдельным агентом:
  контент всё равно проходит через контекст целиком, и основной диалог это
  съедает. Побайтовая сверка после каждой заливки обязательна — в этой сессии
  так нашлись два испорченных символа (`вс蔞-таки`, `по白списку`).
- **Урок #26.** Формулировать уверенность честно. Причина глюка с шифтом была
  подана как установленный факт, а оказалась догадкой; после проверки
  выяснилось, что предполагаемого механизма в коде нет вообще.

---

## 14. Подача в App Store: от отправки до одобрения (18–23.08.2026)

### 14.1 Итог

**23.08.2026 приложение ОДОБРЕНО Apple с первой подачи** (одна итерация
Information Needed + два автоматических флажка). 62 объекта одной заявкой:
версия 1.0 (сборка 36), подписка club.basic.monthly, все book.*/package.*.
Статус: **Pending Developer Release** — выпуск вручную, в магазине появится
после нажатия «Выпустить эту версию». Решение о дате — за Анной (сразу или
1 сентября вместе со стартом сезона).

### 14.2 Как шла проверка

- 18.08: заявка отправлена. Сборка 36 собрана Codemagic (номер билда
  считается автоматически от последнего в TestFlight, pubspec 1.0.0+1
  ни на что не влияет).
- 19.08: Guideline 2.1 Information Needed — стандартный запрос для новых
  приложений: видео с устройства + 8 пунктов информации. НЕ отказ.
  Ответ: текст по всем пунктам + видео 5:12 (iPhone 11 Pro Max, iOS 26.6.1),
  снятое на телефоне жены владельца. Примечания App Review переписаны
  (полный текст — в проектном доке claude/reviewer-accounts.md и в переписке).
- 22.08: два автоматических флажка: (1) нет ссылки на Terms of Use в
  метаданных карточки — добавлена строка «Условия использования (EULA):
  https://api.chitatel.app/legal/terms» в конец русского Описания;
  (2) запрос подтверждения цены «Бегущая с волками» $80.99 — цена
  подтверждена как задуманная.
- 23.08: Approved.

### 14.3 Зомби-подписка песочницы и найденный баг клиента (важно!)

Симптом: на устройстве разработчика «не удалось начать/подтвердить покупку»
подписки под любым аккаунтом приложения. Причина — цепочка:

1. 08.07 владелец купил club.basic.monthly в sandbox под своим аккаунтом
   (g.ahmeteli@gmail.com, originalTransactionId 2000001201795610).
   В TestFlight подписка продлевается РАЗ В СУТКИ (~10:21 UTC) и живёт
   месяцами — «6 продлений и смерть» относится только к dev-sandbox.
2. Пока подписка активна, StoreKit на повторную «покупку» того же продукта
   с этого Apple ID возвращает СУЩЕСТВУЮЩУЮ транзакцию с исходным
   appAccountToken владельца.
3. Серверная защита (аудит 16.08) отвергает чужую транзакцию
   (PURCHASE_INVALID «Покупка принадлежит другому аккаунту») — корректно.
4. ⚠️ БАГ КЛИЕНТА: purchase_provider._verify при ошибке НЕ вызывает
   complete() (задумано против потери оплаченной покупки), поэтому
   отвергнутая ЧУЖАЯ транзакция висит в очереди StoreKit вечно и блокирует
   новые покупки продукта. Реальный сценарий: семья с одним Apple ID и
   двумя аккаунтами приложения.

**Фикс в первое обновление:** при постоянном отказе типа PURCHASE_INVALID
завершать транзакцию (complete), не выдавая доступ. Различать постоянные
отказы (4xx «чужая покупка») от временных (сеть/5xx), при которых транзакцию
оставлять для повтора.

Видео для ревью в итоге снято на чистом устройстве (телефон жены):
TestFlight логинится аккаунтом разработчика, покупки идут через Apple ID
владельца телефона — это штатный способ получить чистую песочницу.
Пароль App Store в кадре не вводить (восстановим по подсветке клавиш):
первая покупка Apple ID на устройстве всегда требует пароль → сжечь этот
запрос «прогревочной» покупкой вне записи, дальше работает Face ID.

### 14.4 Прочее сделанное в эти дни

- Сервер упал 19.08 из-за ребута VPS: pm2 не был в автозапуске. Поднято;
  настроены pm2 save + pm2 startup (systemd, pm2-deploy enabled).
- legal-страницы: полные реквизиты ИП из выписки реестра Грузии
  (Individual Entrepreneur HANNA BUSEL, ИН 302269391, адрес Тбилиси,
  Чугурети, Джавахишвили 91) — privacy.html и terms.html, пометки
  «ВРЕМЕННЫЕ ДАННЫЕ» сняты. На сервере нужен git pull (статика).
- Иконка-монограмма BB: 15 PNG залиты в
  app/ios/Runner/Assets.xcassets/AppIcon.appiconset через веб-интерфейс
  GitHub. ⚠️ Урок #27: create_or_update_file (GitHub MCP) НЕ умеет бинарники —
  кладёт base64-текст вместо PNG, БЕЗ ошибки. Бинарники — только через
  git push или веб-интерфейс.
- club.basic.season НЕ подан (до 1.09 не покупается — отклонили бы).
  Подать отдельно после публикации: покупки после релиза отправляются без
  новой версии приложения. Чтобы продажи стартовали 1.09 — подать в
  двадцатых числах августа.
- Описание, ключевые слова, Age Rating (16+ по выбору владельца), App
  Privacy (7 типов данных, включая Фото и User Content/OpenAI), доступность
  всех продуктов во всех регионах, скриншоты 6.5" (6 шт., сняты с билда 36).
- Обложки книг оставлены издательские (решение Анны, риск 5.2 принят).
  Запасной план готов: типографские обложки в стиле иконки генерируются
  скриптом, подмена через coverImageUrl (https) БЕЗ пересборки.
- План Android создан: docs/ANDROID-PLAN.md (задачи A–H, ~6 недель,
  требования Google сверены с первоисточниками). Работа — в ветке android,
  main не трогать до выпуска iOS.

### 14.5 Что осталось (по убыванию срочности)

1. **Сентябрьский ClubMonth в админке ДО 1.09** — иначе /api/club/current
   отдаст 404 и экран клуба зависнет у ВСЕХ (club_screen.dart ловит только
   SUBSCRIPTION_REQUIRED). Пять минут в админке.
2. Решение Анны о дате выпуска → нажать «Выпустить эту версию».
3. После выпуска: реальная покупка на проде (свой аккаунт, живые деньги),
   проверить вебхуки в проде.
4. Подать club.basic.season (~25.08) чтобы к 1.09 был одобрен.
5. git pull на сервере (legal-страницы с реквизитами).
6. Первое обновление (копилка): фикс завершения чужих транзакций (14.3),
   шифт на регистрации (13.4), «4 цитат/4 анализов» склонение,
   TextCapitalization в полях клуба (13.4), вычистить мёртвый
   debugNowOverride-жест (season_window.dart), PrivacyInfo.xcprivacy —
   добавить NSPrivacyCollectedDataTypePhotosOrVideos (в анкете ASC фото
   заявлены, в манифесте нет).
7. Android по docs/ANDROID-PLAN.md.

### 14.6 Уроки

- **Урок #28.** «Покупка не стартует» на одном устройстве ≠ баг для всех:
  сначала выяснить, ЧЬЯ транзакция держит очередь (Purchase по
  originalTransactionId из лога → userId → email), потом чинить.
- **Урок #29.** Information Needed от Apple — это анкета, а не отказ.
  Отвечать в тот же тред с видео; новая сборка не нужна; после ответа
  нажать активную кнопку отправки, если она есть.
- **Урок #30.** Автофлажок «нет Terms of Use» ловит большинство подписочных
  приложений: ссылка на EULA обязана быть в самом Описании карточки,
  пейвола недостаточно.

---

## 15. Релиз случился, 1.0.1, чистка админки (24–29.08.2026)

### 15.1 Приложение ВЫШЛО в App Store

Выпуск стоял «автоматически» (галочку в 1.0 не переставили) — после
одобрения 23.08 версия ушла в магазин сама. Итог недели БЕЗ анонса:
**37 живых регистраций за 4 дня** (25–29.08), всего 53 аккаунта в базе.
24.08 приложение прошёл живой прогон ревьюера («John Apple»,
privaterelay: купил подписку и book.lolita в своей песочнице).

### 15.2 Версия 1.0.1 (собрана, проверена в TestFlight)

Причина: каталог/книги/пакеты показывали статичный priceUsd из БД — у
пользователей других сторфронтов цифра на кнопке не совпадала с окном
оплаты Apple («выглядит как обман», жалоба из Литвы). Пейвол подписок
изначально брал цену из StoreKit — он был прав.

Состав (все правки в main, версия 1.0.1+1):
- **Живые цены StoreKit** в 6 местах: book_grid_card, book_screen,
  package_card, package_screen, popular_books_section, paywall_sheet.
  Новый провайдер lib/features/payments/providers/store_prices_provider.dart
  (ensure() пачкой, fallback на priceUsd из БД пока StoreKit не ответил).
  Кроссплатформенно: на Android тот же код возьмёт цены Google Play.
- **Подпись про налоги** «Итоговая сумма с учётом налогов — в окне оплаты
  Apple»: последнее предложение легального абзаца пейвола + строка под
  кнопками покупки на экране книги. Формулировка нейтральная намеренно:
  в большинстве стран налог уже в цене, «может быть выше» пугало бы зря.
- **Склонения счётчиков** (цитата/цитаты/цитат и т.п.): дневник, недельный
  и месячный отчёты. Новый хелпер lib/core/utils/plural.dart.

Знания о ценах Apple (проверено на живых кейсах):
- валюту определяет сторфронт Apple ID, НЕ география (литовец с грузинским
  Apple ID видит доллары — это норма);
- в части стран (Грузия, США...) налог добавляется ПРИ списании поверх
  показанной цены — у ВСЕХ приложений, не лечится;
- в большинстве стран (РФ, ЕС...) налог внутри цены — после 1.0.1
  совпадение кнопка=окно=чек полное.

docs/RELEASE.md — пошаговая инструкция выпуска версий (создана).
Правило: багфиксы — автовыпуск, версии под событие — ручной.

### 15.3 Аналитика админки: sandbox отделён от денег

Дашборд показывал $810 «выручки» — ВСЯ она была песочницей (тесты
разработчика, TestFlight с телефона жены, ревьюер Apple). Исправлено
серверно, без сборки приложения:
- Purchase.environment ('production'|'sandbox') — пишется в
  applyTransaction из tx.environment декодированной транзакции;
- admin-analytics: все запросы Purchase с фильтром environment≠sandbox;
- admin-payments: в списке оплат бейдж «sandbox» вместо «Apple» у тестовых,
  выручка шапки тоже фильтрует; имя в строке — ссылка на карточку
  человека (tab('people')+openUser(id), в items добавлен userId);
- scripts/mark-sandbox-purchases.js — разово помечены все 31 накопленные
  покупки ($897.83 ушло из «выручки», реальных продаж не скрыто — их
  ещё не было). ВЫПОЛНЕНО 29.08.

### 15.4 Чистка тестовых аккаунтов

scripts/delete-test-users.js — штатное удаление (копия логики
DELETE /auth/account) для test/test123/testuser/testapp@chitatel.app.
ВЫПОЛНЕНО 29.08. НЕ трогали: appreview*@ (нужны Apple при каждом ревью),
test-premium/basic/expired@ (Марина Л./Оля/Дарья — на них чат клуба),
John Apple (след ревьюера), админов и живых.

### 15.5 Расследование «у Алёны всё куплено» — ложная тревога

Жалоба через Анну: у новой пользовательницы якобы все разборы доступны
и пакеты «куплены». Проверено всё: код доступа, аудио-выдача, флаги
isPreviewAvailable по 50 платным книгам (scripts/check-preview-flags.js —
чисто, открыта максимум 1-я часть), аккаунт Алёны (пустой), дубли,
nginx-кэш (нет). Вывод: «Куплено» на всех пакетах видит только админ
(role=admin → полный доступ by design) — скорее всего Анна смотрела со
своего телефона. Правило для Анны: проверять «как видят люди» только
гостем или обычным аккаунтом. В копилку 1.0.2: админу показывать
«Доступ администратора» вместо «Куплено».

### 15.6 Запуск скриптов на VPS — ВАЖНО

.env лежит в server/ → ВСЕ node-скрипты запускать ИЗ /home/.../server:
`cd server && node src/scripts/имя.js`. Из корня dotenv не найдёт .env,
конфиг упадёт на дефолтный mongodb://localhost:27017 — это Mongo ЧУЖОГО
проекта (reader-bot) на этом VPS, он ответит «requires authentication».
Данные при этом не страдают (защита аутентификацией), но выглядит страшно.

### 15.7 Статус и что осталось

- 1.0.1: собрана (Codemagic), проверена в TestFlight — отправить на
  проверку, выпуск автоматический.
- **Сентябрьский ClubMonth в админке — ДО 1.09**, иначе экран клуба
  зависнет у всех (см. 13.7).
- **club.basic.season — подать на проверку** с заметкой ревьюеру (продаётся
  с 1.09, сейчас в приложении только анонс-баннер). Продукт подаётся
  отдельно от версий.
- Пароль сидового админа anna@chitatel.app — сменить, если ещё не.
- Копилка 1.0.2: фикс завершения чужих транзакций (14.3), шифт (13.4),
  «Доступ администратора» для админа, «Ознакомительный фрагмент» в плеере,
  PrivacyInfo + Photos, вычистить debugNowOverride.
- Android — по docs/ANDROID-PLAN.md, не начат.

### 15.8 Уроки

- **Урок #31.** «Расплодились тестовые» может оказаться живыми
  пользователями. Перед любой чисткой — полный список с почтами;
  правило: удалять только своё (@chitatel.app), и то по белому списку.
- **Урок #32.** Sandbox-выручка неотличима от настоящей, пока не хранишь
  окружение транзакции. Флаг environment надо закладывать в платёжную
  модель с первого дня.
- **Урок #33.** Заявленное поведение сверять с фактическим построчно:
  «цены подтягиваются из StoreKit» оказалось правдой только для пейвола,
  каталог показывал статику из БД (в коде честно лежал комментарий
  «когда подключим StoreKit»).

## 16. Админка: загрузка клуба месяца (01.09.2026)

Анна попросила показать, как заводить клуб через админку (есть аудио-
приветствие, картинка, описание). При разборе кода выяснилось, что форма
клуба не отражала эксклюзивность клубного разбора, которую держит сервер.

### 16.1 Что было не так

- Выпадашка «Разбор клуба» показывала ВЕСЬ каталог, а newClub()
  подставлял первую книгу каталога по умолчанию. Сохранение клуба ставит
  выбранной книге isPartOfClub → она исчезает из каталога, «Популярного»
  и главной для всех. Одно неверное нажатие — разбор пропал из продажи.
- «Создать разбор для клуба» открывал обычный редактор с «Тип:
  Платный/Бесплатный», ценой и категориями. Для клубного разбора это мусор,
  а «Бесплатный» — дыра: books.js checkPartAccess при isFree отдаёт аудио
  всем без авторизации, в обход подписки.
- Удаление клуба сбрасывало isPartOfClub → опубликованный клубный разбор
  всплывал в общем каталоге.

### 16.2 Что сделано (только сервер + админка, сборка не нужна)

- admin-club.js: assertClubBook — к клубу привязывается только разбор с
  isPartOfClub (создан кнопкой «Создать разбор для клуба»), иначе 400 с
  понятным текстом. unmarkBookIfUnused снимает только clubMonth, флаг
  isPartOfClub остаётся навсегда (эксклюзив = эксклюзив).
- admin-catalog.js: applyClubExclusive — для isPartOfClub принудительно
  isFree=false, priceRub/Usd/Byn=null, appleProductId=null (POST и PATCH).
- admin/index.html: форма клуба — один выбор «Месяц» (type=month) вместо
  двух дат (сервер и так нормализует к полному месяцу); в списке только
  клубные разборы, не занятые другим клубом, черновики помечены; превью
  предупреждает «черновик — опубликуйте» (без публикации /books/:id → 404,
  «Слушать разбор» не откроется). Редактор клубного разбора без
  типа/цены/категорий, с баннером-шагами и кнопкой «К клубу месяца →»
  (возвращает в форму клуба с этим разбором и выбранным до этого месяцем).
  Подсказка по частям: приветствие — часть 1 «Приветствие от Анны».

### 16.3 Порядок для Анны

Клуб месяца → «+ Новый клуб» → выбрать месяц → «＋ Создать разбор для
клуба» → название/автор/описание → Сохранить → обложка (jpg/png/webp/heic
до 8 МБ, 600×900) → аудио-части (часть 1 = приветствие, дальше разбор) →
«Опубликовать» → «К клубу месяца →» → «Создать клуб». Голосовые в чат
через админку не грузятся (только запись из приложения, до 3 мин).

- **Урок #34.** Если сервер хранит инвариант (клубный разбор эксклюзивен),
  форма админки обязана его показывать и не давать нарушить: список
  «всего каталога» + автоподстановка первого элемента = мина для
  нетехнического админа.

## 17. 1.0.2: клуб «прошлый месяц → новый», чужие транзакции, мелочи (01.09.2026)

Сборка 1.0.2 (pubspec 1.0.2+1, codemagic --build-name=1.0.2). Только клиент;
серверные фиксы этого дня (поиск без клубных разборов `83ec543`,
appleProductId в /home `90bf37c`, эксклюзивность клуба в админке `c2d7d12`)
уже задеплоены отдельно.

### 17.1 Клуб: подписчица прошлого месяца и новый клуб

Симптом (Гио, 01.09, аккаунт с оплаченным августом): тап по сентябрю в
переключателе и по карточке «Клуб месяца» на главной — «ничего не
происходит». Причина — не подписка, а два места в club_screen.dart:
1. M1 (вход без доступа → открыть оплаченный архив) срабатывал повторно:
   ручной выбор текущего клуба сбрасывал пометку «архив уже подставляли»,
   приходил 403, архив подставлялся снова → петля назад в август.
2. Баннер «Подписка истекла — доступен архив» серый и без названия нового
   клуба — не читался как «вы в архиве, сентябрь по подписке».

Решение (продуктовое, согласовано с Гио — вариант «архив при входе, пейвол
по явному выбору»; предпросмотр «О клубе» до пейвола отложен):
- clubManualCurrentProvider: ручной выбор текущего (переключатель, карточка
  на главной, баннер архива) → при 403 архив НЕ подставляется, показывается
  пейвол. Живёт до перезапуска/смены аккаунта; сбрасывается выбором другого
  клуба. Из dispose() не трогаем (фаза перестроения дерева).
- clubArchiveFallbackProvider: id подставленного архива переехал из State в
  провайдер — ShellRoute пересоздаёт ClubScreen при каждом входе во вкладку,
  локальное поле терялось (баннер пропадал).
- Пейвол во вкладке рисуется ПОД шапкой-переключателем (_buildPaywall):
  «КЛУБ СЕНТЯБРЯ · Грёзы об Эдеме ˅» + PaywallScreen(showClose:false,
  showAppBar:false — новый флаг, вторая шапка не нужна). Из пейвола назад в
  архив — один тап. Название текущего клуба — из clubListProvider.current.
- Баннер архива терракотовый и явный: «Это архив августа. Клуб сентября
  «…» — оформить подписку», тап = ручной выбор текущего.
- ClubMonthCard (главная) стал ConsumerWidget: перед go(club) ставит manual,
  сбрасывает fallback и выбранный клуб.
- Хелперы clubMonthLabel / clubMonthGenitive вынесены на уровень файла.

### 17.2 Чужие транзакции (14.3) — завершаем только при 403

PurchaseService.isForeignAccountError: DioException, status 403, code
PURCHASE_INVALID («Покупка принадлежит другому аккаунту»). В обоих _verify
(purchase_provider, product_purchase_provider) на такую ошибку — complete()
транзакции + сообщение «…под другим аккаунтом». Все прочие ошибки (400
подпись, сеть, 5xx) — как раньше, транзакцию не завершаем. Это не даёт купить
подписку второй раз под другим аккаунтом на том же Apple ID (правило Apple),
но убирает «отравленную» очередь, из-за которой не работали другие покупки.

### 17.3 Мелочи

- book_parts_list: часть №1 с названием «Приветствие…» → подпись
  «Приветствие» вместо «Часть 1»; если название ровно «Приветствие» — под ним
  «Анна Бусел». Когда приветствие есть, остальные части показываются со
  СДВИГОМ (part.number - 1): файл, названный в админке «Часть 1», лежит под
  номером 2 и показывается как «Часть 1». Подзаголовок скрывается, если он
  дублирует подпись («Часть 1 · Часть 1»). Капса в списке частей нет — он
  только в шапке клуба (там .toUpperCase() был и раньше).
- book_screen: isPartOfClub без доступа → _ClubOnlyActions («Разбор
  книжного клуба — доступен участницам по подписке» + «Оформить подписку на
  клуб» → PaywallScreen) вместо неактивной «Купить разбор».
- isAdminProvider (profile_provider): «Доступ админа» вместо «Куплено» в
  book_grid_card / package_card, «Доступ администратора» на экране пакета.
  Смотрится только при уже имеющемся доступе — гостю профиль не грузим.

### 17.3a Части разбора переехали во вкладку клуба (после первой сборки)

Гио на живом сентябрьском клубе: «два одинаковых экрана». Вкладка «Разборы»
показывала обложку + описание + кнопку «Слушать разбор» → BookScreen с той же
обложкой и описанием, а список частей там ниже сгиба и при ОДНОЙ части вообще
скрыт (правило каталога «одна часть — списка нет»). Пока залито только
приветствие (8 мин), кнопка «Слушать разбор» включала приветствие, и нигде не
было сказано, что разбора ещё нет.

- club_about_tab: собирает BookModel.fromJson(bookJson) (GET /club/current
  отдаёт документ книги целиком) и рисует ТЕ ЖЕ виджеты, что каталог —
  ListenButton (зелёная) + BookPartsList (isPurchased: true, замков нет: без
  доступа в клуб не пускает пейвол, архив открыт целиком). Тап по части →
  плеер с этой части (extra startPart/startPosition), кнопка → плеер без extra
  (продолжает с сохранённого места, audio_service тянет /progress/:bookId) и
  называется «Слушать приветствие», пока приветствие — единственная часть.
  Нет аудио — кнопка в неактивном виде «Аудио загружается».
- ListenButton вынесена из book_screen (_GreenListenButton) в
  shared/widgets/listen_button.dart — одна кнопка на каталог и клуб.
- Заголовок «О книге» в клубе убран: на карточке разбора описание идёт просто
  текстом (bodyMedium/textSecondary) — теперь так же и в клубе.
- Побочно: в клубе читался несуществующий ключ 'coverGradient' — всегда
  подставлялся дефолтный градиент; теперь coverGradientColors из модели.
- Строка «Новые части разбора выходят по понедельникам» показывалась по
  условию club.partSchedule (расписание в админке не заполняется НИКОГДА —
  сервер сохраняет пустой список), то есть не показывалась ни разу. Теперь
  условие — идёт ли месяц клуба (в архиве не обещаем новые части).
- shared/utils/part_labels.dart — общий хелпер подписей (isGreetingPart,
  hasGreetingPart, partLabel, partSubtitle); book_parts_list переведён на него,
  чтобы клуб и карточка не разъехались.
- BookScreen у клубной книги остаётся только для прямых заходов по id (там
  _ClubOnlyActions). Прогресс НЕ трогали: сохранение и чтение как было, на
  экранах он по-прежнему заглушён (полоска и галочки) — доделка в 1.0.3
  сразу для каталога и клуба.
- Процесс загрузки клуба в админке не меняется (CLUB-UPLOAD-GUIDE в силе).

### 17.3b Клубный разбор не «дослушан», пока идёт его месяц (сервер)

Симптом (01.09): Гио послушал 8-минутное приветствие — единственную залитую
часть — и «Грёзы об Эдеме» попали в дослушанные разборы («Мой прогресс» и
счётчик booksCompleted). Правило было общее: прослушаны все части = дослушан.
Для каталога верно, для клуба нет — части добавляются в течение месяца.

progress.js: хелпер isBookCompleted(book, listenedCount) — клубный разбор
ТЕКУЩЕГО месяца (book.isPartOfClub && book.clubMonth === ключ текущего месяца
в UTC) дослушанным не считается никогда; прошлые клубные месяцы и обычные
разборы — как раньше. Применён в /progress/stats (booksCompleted) и
/progress/list (isCompleted); в выборки добавлены поля isPartOfClub/clubMonth.
Только сервер — деплой git pull + pm2 restart, сборка не нужна.

### 17.4 Чек-лист TestFlight 1.0.2 (до отправки в Apple)

Аккаунт А — оплачен только август (у Гио): вход во вкладку «Клуб» → август
с терракотовым баннером про сентябрь; переключатель → сентябрь → пейвол с
шапкой «КЛУБ СЕНТЯБРЯ»; шапка → август → открылся; уход на другую вкладку и
возврат — не залипает; карточка на главной → пейвол с шапкой; тап по баннеру
→ пейвол. Аккаунт Б — чистый: вкладка «Клуб» → пейвол с шапкой; карточка на
главной → пейвол. Админ — клуб, каталог («Доступ админа»), пакет. Клубный
разбор: в поиске нет; по прямой ссылке без доступа — «Оформить подписку на
клуб». Части: во вкладке клуба список виден сразу, «Приветствие / Анна Бусел · 8 мин»,
дальше «Часть 1», «Часть 2»… (нумерация без дублей и без сдвига относительно
названий из админки); тап по строке запускает эту часть; кнопка сверху
продолжает с сохранённого места; при одном приветствии кнопка называется
«Слушать приветствие». Мини-плеер и «Продолжить слушать» на главной работают. Телефон с
чужой подпиской: покупка разбора проходит, в логе сервера один
PURCHASE_INVALID без повторов при перезапусках.

### 17.5 Копилка (не в 1.0.2)

Живой прогресс на экранах (полоска «прослушано N%» и галочки на частях) —
сейчас заглушки с Фазы 2, нужен провайдер прогресса по книге с обновлением
после прослушивания, сразу для каталога и клуба; предпросмотр «О клубе» для
клуба без доступа (перед пейволом) — нужен новый эндпоинт без проверки
доступа; «Ознакомительный фрагмент» в плеере;
PrivacyInfo + Photos; вычистить debugNowOverride; голосовое приветствие в
чат файлом из админки (сейчас только запись из приложения ≤3 мин).

- **Урок #35.** Локальное состояние экрана под ShellRoute живёт до первого
  переключения вкладки. Всё, что должно пережить вход-выход (подстановка
  архива, ручной выбор), — в провайдер, и сбрасывать вместе с аккаунтом.
- **Урок #36.** «Ничего не происходит» при тапе почти всегда означает
  «произошло и откатилось». Искать не отсутствие обработчика, а второй
  обработчик, который возвращает состояние назад.
