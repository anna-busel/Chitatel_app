# AI-CONTEXT-2 — ЧИТАТЕЛЬ (продолжение)

> **ЭТО ВТОРОЙ ФАЙЛ КОНТЕКСТА. Первый — `docs/AI-CONTEXT.md` — разросся (52KB).**
>
> **Порядок чтения для AI в начале сессии:**
> 1. Сначала `docs/AI-CONTEXT.md` — вся база: проект, стек, Фазы 1-2, чат 4.1-4.8, аудио, уроки #1-19, правила кода, среда, решения до 16.05.2026. **Это фундамент, читать обязательно.**
> 2. Потом **этот файл** (`AI-CONTEXT-2.md`) — свежий прогресс с 16.05.2026: задачи 4.9-4.12, 1.3/1.7/1.8, новые баги/фиксы, уроки #20+, актуальный статус.
>
> **Прогресс фиксировать ДАЛЕЕ ТОЛЬКО В ЭТОМ ФАЙЛЕ.** Первый не редактировать (он стабильный архив-фундамент). Когда и этот разрастётся — заведём AI-CONTEXT-3 по той же схеме.
>
> Правила и роль — в Project Instructions проекта Claude (не в репо).

---

## ТЕКУЩИЙ СТАТУС (на 12.06.2026)

**Фаза:** 4 — **ЗАВЕРШЕНА И ПРОТЕСТИРОВАНА.** Бэкенд 4.1-4.12 ✅. Фронт 4.1-4.12 ✅. Все 12 задач + Telegram-навигация + фиксы — в `main`, рабочие.

**✅ ГОТОВО 12.06.2026 — задачи 1.7 (Apple Sign In, фронт) + 1.8 (гостевой режим):**
- ✅ **1.3 Apple Sign In (бэк)** — ранее, 11.06.2026 (`bcb3df1`).
- ✅ **1.7 Apple Sign In (Flutter-сторона)** — кнопка «Sign in with Apple» реально вызывает `signInWithApple()` (была заглушка-snackbar). Пакет `sign_in_with_apple ^6.1.4` (запиннен под Xcode 15.2/Flutter 3.22.3). Шлёт identityToken + fullName (первый раз) на `POST /api/auth/apple`.
- ✅ **1.8 Гостевой режим (Apple 5.1.1(v))** — заметная опция «Продолжить без регистрации»; локальный прогресс гостя в SharedPreferences (фикс бага «позиция не запоминается» — был тихий 401); GuestGate (мягкое приглашение войти) на табах Клуб/Профиль; бесплатный контент открыт гостю. Бэкенд гостя был готов с Фазы 2 — менять не пришлось.

**Подтверждено тестом юзера (19.05.2026):**
- ✅ **4.12 Голосовые** — запись/отправка/воспроизведение работает (под Анной). Layout-баг RenderFlex исправлен, краша нет. record 4.x рабочий.
- ✅ **4.6 Картинки** — отправка фото работает.

**НЕ подтверждено отдельно юзером** (написано и в `main`, но юзер на этом тесте явно не называл — НЕ значит «сломано», значит «не верифицировано вручную»): 4.9 mentions автокомплит @, 4.10 закрепы long-press, 4.11 read receipts, real-time после диалогов/табов, поведение под участницей. Бэк стабилен; фронт по тем же паттернам что прошли тест. Рекомендуется добегать чек-лист при следующем заходе в чат. **Задачи 1.7/1.8 — тест Apple Sign In только на устройстве/TestFlight (Фаза 7); гостевой режим тестируется в симуляторе.**

**Следующее:** Фаза 5 (ИИ-дневник 5.1-5.3) → Фаза 3 (платежи) → Фаза 6 (полировка) → Фаза 7 (TestFlight).

**Блокеры:** **Apple Dev аккаунт — ✅ ЗАРЕГИСТРИРОВАН 11.06.2026** (Individual/ИП Hanna Busel; разработчик добавлен Admin; App ID `app.chitatel.ios` + Sign in with Apple; запись приложения создана; DSA на проверке; Team ID `3GS6F87RKZ`). 1.8 (guest mode) — ✅ снят (готов). Сборка для TestFlight — через Codemagic (машина 2017/Xcode 15.2 не тянет Xcode 26).

---

## ✅ ЧАТ КЛУБА — ЗАВЕРШЕНО 16-19.05.2026 (Фаза 4: 4.9-4.12 + навигация + фиксы)

Всё в `main`. Бэкенд полностью готов и стабилен. Фронт 4.9-4.12 готов. Голосовые и фото подтверждены тестом 19.05.2026.

### Сводка задач 4.9-4.12

| Задача | Бэк | Фронт | Тест | Суть |
|--------|-----|-------|------|------|
| 4.9 Mentions @Анна | ✅ | ✅ | ⏳ не верифиц. отдельно | Вариант А: упоминать можно ТОЛЬКО Анну (role=admin). `GET /:clubMonthId/mentionable`, `sanitizeMentions()` фильтрует подделку. Автокомплит `@` в инпуте, подсветка `@Имя` в bubble |
| 4.10 Закрепы | ✅ | ✅ | ⏳ не верифиц. отдельно | Только Анна-admin, 1 закреп на клуб (`ClubMonth.pinnedMessageId`), WS `chat:pin_changed`. Long-press → «Закрепить»/«Открепить» (пункт только админу). Баннер сверху |
| 4.11 Read receipts | ✅ | ✅ | ⏳ не верифиц. отдельно | `POST /chat/read {messageIds[1-100]}`, `$addToSet readBy`, без WS (фоновая метрика для Анны). Фронт markRead дебаунс 2с, Set `_readSent` |
| 4.12 Голосовые | ✅ | ✅ | ✅ **ПОДТВЕРЖДЕНО 19.05** | ТОЛЬКО Анна-admin. AAC m4a, 3 мин, signed URL через AUDIO_SECRET. Запись→отправка→воспроизведение работает |
| 4.6 Картинки | ✅ | ✅ | ✅ **ПОДТВЕРЖДЕНО 19.05** | Отправка фото работает (повторный тест после real-time фикса) |

### Telegram-навигация к закрепу/reply — РЕАЛИЗОВАНО

Полноценный переход как в Telegram: тап по баннеру закрепа / reply-превью → если сообщение в окне — скролл+подсветка; если нет — `GET /chat/context/:messageId` (radius 1-30, def 15) → перестройка ленты вокруг цели + подсветка (Timer 1800мс terracotta-обводка) + плавающая кнопка «вниз» (`_JumpDownButton`, индикатор если есть новее за окном).

### Картинка в bubble — БЕЗ оранжевой рамки (фикс UX)

Без подписи = bubble это сама картинка (без фона/padding), время/имя поверх на чёрной полупрозрачной подложке. С подписью = картинка fullWidth сверху + подпись снизу на цветном фоне. Fullscreen zoom по тапу (Hero + InteractiveViewer).

### Real-time баг — ИСПРАВЛЕН (коммит `49b5b41`)

**Симптом:** добавление/удаление/фото не в реальном времени, чат «залипал» после диалогов/смены вкладок.
**Причина (найдена чтением кода):** `chat_tab.dispose()` рвал singleton-сокет `_socketService?.disconnect()` при любом пересоздании виджета (а оно частое — диалоги, табы).
**Фикс:** убран `disconnect()` из `dispose()`. Виджет только `_socketSub?.cancel()`; соединением рулит провайдер; `connect()` идемпотентен. Фото-отправка после фикса подтверждена рабочей 19.05; полную real-time проверку (удаление/после диалогов) рекомендуется добегать отдельно.

### Конфликт пакета `record` (4.12) — РЕШЁН

**Проблема:** `record: ^5.1.2` тянул `record_linux 0.7.2` несовместимый с `record_platform_interface` → iOS-сборка падала `RecordLinux is missing implementations` (Flutter компилирует весь граф зависимостей, даже Linux-код, ненужный на iOS).
**Ошибка Claude по пути:** выдумал несуществующую версию `record_platform_interface 1.0.4` в `dependency_overrides` (нарушил правило «не угадывать») — `pub get` упал «doesn't match any versions». Признал, откатил (`611bd26`).
**Решение:** перешли на `record: ^4.4.4` (коммит `c8bb44d`) — цельный пакет без проблемного распада на под-пакеты, ставится на Flutter 3.22.3. `voice_recorder.dart` переписан под record 4.x API (`b1ecc01`): класс `Record()` вместо `AudioRecorder()`, параметры прямо в `start(path:, encoder: AudioEncoder.aacLc, bitRate:64000, samplingRate:44100)` без `RecordConfig`, нет `numChannels`, `samplingRate` не `sampleRate`. **record 4.x СОБРАЛСЯ и ПРОТЕСТИРОВАН успешно — запись работает.**

### Layout-баг VoiceRecorder (RenderFlex unbounded width) — ИСПРАВЛЕН И ПОДТВЕРЖДЁН

**Симптом:** нажал микрофон под Анной → разрешение появилось → виджет упал `RenderFlex children have non-zero flex but incoming width constraints are unbounded` (стектрейс → `voice_recorder.dart` Row со `Spacer()` в режиме записи, родитель не дал ограниченную ширину).
**Причина (из стектрейса, не угадана):** в режиме записи VoiceRecorder = Container→Row со `Spacer()` (flex), но в `chat_input` стоял как обычная кнопка БЕЗ `Expanded` → unbounded width + flex = краш.
**Фикс (2 файла):**
- `voice_recorder.dart` (`d3be72a`) — добавлен колбэк `onRecordingStateChanged(bool)`, вызывается в `_setRecording()` (в `_start`/`_stopAndSend`/`_cancel`).
- `chat_input.dart` (`c5f5d17`) — добавлен `bool _isRecordingVoice`; единый инстанс `VoiceRecorder` (через переменную — чтобы State не пересоздавался при перестройке); в режиме записи строка ввода = `[Expanded(child: voiceRecorder)]` (скрепка/TextField/SendButton скрыты); иначе обычная раскладка. Reply-плашка и счётчик скрыты при записи.
**Статус:** ✅ **ПОДТВЕРЖДЕНО ТЕСТОМ 19.05.2026** — краша нет, цикл запись→отправка→воспроизведение голосового работает под Анной.

---

## ПРОДУКТОВЫЕ РЕШЕНИЯ 16-17.05.2026 (зафиксированы)

| Дата | Решение | Обоснование |
|------|---------|-------------|
| 16.05 | **Mentions (4.9) — вариант А: упоминать ТОЛЬКО Анну (role=admin)** | Не групповой флуд-чат, а сообщество вокруг разбора. Бэк `sanitizeMentions()` фильтрует mentions[] оставляя только админов (защита от подделки клиентом). Эндпоинт `GET /:clubMonthId/mentionable` |
| 16.05 | **Голосовые (4.12) — ТОЛЬКО Анна-admin** | Формат ведущей. Бэк: `role==='admin'` в `POST /chat/voice` (код `VOICE_ADMIN_ONLY`) + блок `type=voice` в обычном `POST /chat` (антиобход 403). Фронт: VoiceRecorder рендерится только при `isAdmin && onSendVoice != null` |
| 16.05 | **Закреп (4.10) — только Анна-admin, 1 на клуб** | `ClubMonth.pinnedMessageId`, WS `chat:pin_changed`. Long-press пункт «Закрепить/Открепить» только админу |
| 16.05 | **Read receipts (4.11) — без WS** | Фоновая метрика для Анны. `$addToSet readBy`. Фронт дебаунс 2с |
| 16.05 | **Переход к закрепу/reply — полноценно как Telegram** | Догрузка контекста (`GET /chat/context/:messageId`) + подсветка цели + кнопка «вниз». Не «скролл только если в окне» |
| 16.05 | **Картинка в bubble БЕЗ оранжевой рамки** | Без подписи = чистая картинка, мета поверх на тёмной подложке. С подписью = картинка + подпись на фоне |
| 17.05 | **`record` 4.x, не 5.x** | 5.x раскладывается на под-пакеты с несовместимым графом на Flutter 3.22.3. 4.4.4 цельный |
| 12.06 | **Apple Sign In фронт (1.7) — `sign_in_with_apple` 6.1.x, не latest** | 8.0.0 требует Flutter 3.41+, 7.x таргетит Swift 6/Xcode 16; машина Xcode 15.2. 6.1.x: min Flutter 3.19.0, Xcode 15 И 16 (урок #27) |
| 12.06 | **Гостевой режим (1.8) — заметная опция + локальный прогресс + GuestGate** | Apple 5.1.1(v): бесплатное без аккаунта. «Продолжить без регистрации» видимая; прогресс гостя локально (SharedPreferences); Клуб/Профиль за мягким приглашением войти |
| 12.06 | **Авто-миграция guest→сервер прогресса при логине — НЕ делаем сейчас** | Минимальный код. Гость локально, при логине стартует с серверного прогресса. Перенос локальной позиции в сервер — опционально позже (PENDING) |
| ранее | Edit 15 мин; soft-delete остаётся в ленте; reactions 1 юзер=1 (6 эмодзи); запрет ссылок вариант А (Анна может); reply-UX Telegram (превью всегда видно) | См. AI-CONTEXT.md, секция РЕШЕНИЯ — без изменений |

**PENDING-вопрос к Анне (новый):** обложка/название клуба = как у книги месяца или отдельный арт? Влияет на схему ClubMonth и визуальный переключатель клубов (не дорабатывать пока не ответит).

---

## РЕШЕНИЯ ПО APPLE DEVELOPER АККАУНТУ (19.05.2026 — ответ на вопрос 8.2)

Обсуждено и согласовано с юзером. Само создание аккаунта — действие Анны (инфраструктура, не код). Факты проверены по официальной документации developer.apple.com.

| Решение | Обоснование |
|---------|-------------|
| **Тип аккаунта — Individual / Sole Proprietor** (НЕ Organization) | У Анны ИП. Apple классифицирует ИП как sole proprietor → путь Individual. D-U-N-S номер НЕ нужен, проверка быстрая (часы–пара дней). Organization требует юрлица (ООО) + D-U-N-S + недели проверки. |
| **Продавцом в App Store будет ФИО Анны, не бренд «ЧИТАТЕЛЬ»** | Особенность статуса Individual/ИП у Apple. Название приложения «ЧИТАТЕЛЬ» — отдельно (это не «продавец»). Сменить на бренд можно только через ООО. Юзер принял. |
| **Apple ID — отдельный новый ящик под проект** (НЕ личная почта Анны, НЕ общий проектный email) | Developer привязан к Apple ID навсегда (нельзя перепривязать/слить). Личную почту с iCloud мешать нельзя. Юзер решил: отдельный новый ящик, НЕ общий. |
| **Разработчика (g.ahmeteli@gmail.com) добавляют официально: App Store Connect → Users and Access → роль Admin** | Подтверждено офиц. документацией Apple: Individual-аккаунт может пригласить до 50 человек в App Store Connect по email. Пароль Анны передавать НЕ нужно — у разработчика свой Apple ID и своя 2FA. Admin даёт: билды, TestFlight, метаданные, отправку на ревью, аналитику. |
| **Чего Admin НЕ может в Individual:** подписывать юр.соглашения / продлевать членство (только Анна-владелец, раз в год) + раздел Certificates, Identifiers & Profiles недоступен | Закрывается автоматической подписью Xcode под аккаунтом Анны — для обычного цикла релиза не мешает. Инженерная оценка (не письменная гарантия Apple), на практике почти всегда достаточно. |
| **Small Business Program (15% вместо 30%) — оформлять ПОЗЖЕ, на этапе Фазы 3 (платежи)** | Бесплатно, отдельно от $99. Подаётся только после принятия Paid Applications agreement в App Store Connect (этап Фазы 3). Рассмотрение до месяца+, ставка 15% включается через 15 дней после конца фискального месяца одобрения. Анна-новый разработчик квалифицируется автоматически. |
| **Налоговая/банковская анкета (Tax Forms/W-8BEN + Banking)** — этап Фазы 3, не сейчас | Для не-US ИП отдельная процедура. Вопрос к бухгалтеру Анны. На этапе регистрации аккаунта НЕ требуется. |

**Памятка для Анны создана:** пошаговая инструкция (регистрация Individual + приглашение разработчика Admin + блок «если что-то не так» + справка-обоснования). Отдана юзеру через чат (НЕ в репо). Достаточна для гладкого сценария; при нестандартной верификации Apple/отклонении карты — юзер на подхвате (подписи кнопок Apple могут отличаться — меняются, в памятке закрыто «скриншот → разработчику»).

**Инфраструктура (домен/сервер) — НЕ этап Apple, отложено до деплоя (задачи 0.3/0.4):**
- **Namecheap (домен):** оформлять на Анну/ИП; разработчику — штатный «Share Access» (Domain List → Manage → Sharing & Transfer → Share Access, права Advanced DNS). Без передачи пароля; разработчику нужен свой бесплатный аккаунт Namecheap. Отзывается в 1 клик.
- **Contabo (VPS):** аккаунт/оплата на Анне (владение); разработчику — SSH-доступ к самому серверу (свой ключ; приватный у разработчика; Анна SSH НЕ создаёт). Стартовый root-доступ для первичной настройки допустим (это доступ к серверу, не передача аккаунта Contabo).
- Принцип: активы проекта (Apple, домен, сервер) — на владельце (Анна/ИП), разработчику технический доступ. Репозиторий и бесплатные регистрации — общие, оставить как есть, НЕ переоформлять.
- Точные шаги панелей Contabo/Namecheap проверять актуально на момент деплоя (меняются), не по памяти.

---

## РЕШЕНИЯ ПО СБОРКЕ И МАШИНЕ (TOOLCHAIN) — 11.06.2026

Обсуждено с юзером. Факты проверены web-поиском по официальным источникам (Apple Developer news / «Upcoming requirements» + страница цен Codemagic, обновлена 07.05.2026), НЕ по памяти.

**Уточнение по машине (важно — раньше в записях стояло «2018», по факту 2017):** рабочая машина — **MacBook Pro 13″ 2017, 2×Thunderbolt 3, Intel Core i5 2.3 GHz (2 ядра), 8 GB RAM**. Потолок macOS — **Ventura (13)**, и он уже стоит. Выше (Sonoma 14 / Sequoia 15 / Tahoe 26) не поднимается — Apple официально режет всё старше 2018 (Sonoma/Sequoia требуют MacBook Pro 2018+). В AI-CONTEXT.md и в секции «РАБОЧАЯ СРЕДА» раньше значилось «2018» — это ошибка записи. Первый файл — неизменяемый архив, не правим; корректное значение зафиксировано здесь и в «РАБОЧАЯ СРЕДА» этого файла.

**Последствие для релиза:** с **28.04.2026** Apple принимает в App Store Connect только билды, собранные **Xcode 26 + iOS 26 SDK** (требование уже в силе). Xcode 26 требует минимум **macOS Sequoia 15.6**, которая на эту машину не встаёт (2017 ≤ Ventura). Замкнутый круг → **апгрейдом не лечится, железо на потолке.** Значит:
- **Локально (на этой машине) — только разработка:** Xcode 15.2 на Ventura, симулятор, физический iPhone примерно до iOS 17. Фазы 4/5 идут как есть, сегодня ничего не блокирует.
- **Сборку для TestFlight/App Store на этой машине сделать НЕЛЬЗЯ.**

**Решение — сборку/релиз делать через облачный CI Codemagic** (заточен под Flutter; в наличии маки с Xcode 26.0–26.5). Он собирает подписанный билд у себя и заливает в App Store **под нашим Apple-аккаунтом**. Сам Apple-аккаунт полностью рабочий — не хватало только машины под Xcode 26, Codemagic закрывает ровно это. «Собрать на аккаунте» — так не бывает: собирают на маке, аккаунт лишь принимает готовый файл.

Условия Codemagic (личный аккаунт, проверено на офиц. странице цен 07.05.2026):
- **500 бесплатных минут/мес** на маках macOS M2, обнуляются 1-го числа. Для одного приложения с нечастыми обновлениями — с запасом (одна iOS-сборка ~15–30 мин → ~15–30 сборок/мес бесплатно).
- Сверх 500 — только если сам включишь биллинг: macOS M2 **$0.095/мин**. **Без привязанной карты списать нечего** — при исчерпании 500 минут сборки просто встают до следующего месяца. Сюрприз-счёта быть не может.
- **Как считаются минуты:** реальное время работы мак-машины на одной сборке. Падение сборки **из-за нашего кода/настроек — минуты списываются**; падение из-за сбоя самого Codemagic или таймаута — НЕ списываются. Личный план = **1 сборка за раз** (параллельные сборки = $49/мес за концаренси — не нужны).
- Тесты на симуляторе при разработке = локально на ноуте, **минуты НЕ тратятся**. Тестировщики гоняют через TestFlight на своих телефонах = сторона Apple, минуты НЕ тратятся. Минуты уходят только на **сборку/заливку каждой новой версии** (~20–30 мин/сборка). Первый месяц может уйти больше — настройка подписи часто идёт с нескольких неудачных (а они считаются).
- **Безопасность:** Codemagic — известный сервис (Nevercode), **SOC 2 Type II** аудит. Пароль Apple ID НЕ передаём — подпись/заливка через **отзываемый App Store Connect API key**. Доступ к GitHub-репозиторию — тоже отзываемый в 1 клик. Срок хранения артефактов на личном аккаунте — 30 дней (старые `.ipa`/`dSYM` качать к себе, если нужны для отладки старых версий).

**Путь апгрейда на будущее:** аренда мака ($20–30/мес, MacinCloud — управляемый план, БЕЗ root-доступа, US-латенси из Грузии) — только как временный мост «здесь и сейчас нужен Xcode 26, а покупать не готовы»; скорее всего НЕ понадобится. Когда (и если) старый ноут перестанет тянуть саму разработку (новые версии Flutter/Xcode потребуют свежую macOS) — брать **б/у Mac mini M1 (~$350–450 разово)**: окупается за год-полтора против вечной аренды, свой полноценный мак без задержек удалёнки. Mac mini — не конкурент Codemagic, а следующий шаг.

**Вывод/рекомендация:** на старте и до выхода в App Store — **Codemagic бесплатно** (лучший вариант под вводные: одно приложение, редкие обновления, ограниченный бюджет, старое железо). Покупка Mac mini — апгрейд при упоре в потолок разработки. Аренда — запасной мостик.

**Тайминг:** всё это нужно только к **Фазе 7 (TestFlight)**. Сейчас ничего покупать/подключать не надо — разработка продолжается на текущем Mac.

---

## APPLE-АККАУНТ ЗАРЕГИСТРИРОВАН + ЗАДАЧА 1.3 APPLE SIGN IN — 11.06.2026

### Apple Developer аккаунт — ОФОРМЛЕН (портал, действия юзера под Анной)
Пройдено вместе с юзером в этой сессии (раньше плана — пункт 8.2 закрыт):
- **Тип — Individual/Sole Proprietor**, продавец **Hanna Busel** (ИП Грузия, ID 302269391, рег. 09.03.2023, юр-адрес Tbilisi, Chugureti, Iv. Javakhishvili str. N91). Соглашение о бесплатных приложениях активно (10.06.2026–07.06.2027).
- **Разработчик добавлен как Admin** (App Store Connect → Users and Access, под своим Apple ID, не под паролем Анны). Account-Holder-only за Анной: Paid Apps agreement, Request access к App Store Connect API (для Codemagic), продление членства.
- **App ID `app.chitatel.ios`** зарегистрирован (developer.apple.com → Identifiers), capabilities: **Sign in with Apple**, Push Notifications, Associated Domains. Apple Pay НЕ ставили (для цифровых подписок нужен IAP, не Apple Pay). **Team ID: `3GS6F87RKZ`**.
- **Запись приложения «Читатель»** создана в App Store Connect (Bundle ID app.chitatel.ios, язык русский, SKU chitatel-ios-001). Метаданные/скриншоты/цены НЕ заполнялись — Фаза 7.
- **DSA (EU trader status)** подан: статус «продавец/трейдер» + контакты ИП, загружена выписка из реестра ИП. Статус «на проверке» (27 стран ЕС). Контакты публичны на странице ЕС; телефон/почту юзер уточняет с Анной (правятся в любой момент: Business → DSA → Edit, верификация по коду).
- Не делать сейчас (свои фазы): App Store Connect API-ключ для Codemagic (Фаза 7; Request access — Анна), Paid Apps + IAP + Small Business 15% (Фаза 3, Анна), APNs `.p8` (Фаза 6).
- ⚠️ **Машина для сборки** — см. секцию «РЕШЕНИЯ ПО СБОРКЕ И МАШИНЕ»: 2017 MBP не поднимет Xcode 26, релиз через Codemagic. Аккаунт рабочий, не хватает только сборочной машины (Codemagic закрывает).

### Задача 1.3 Apple Sign In (бэкенд) — ✅ ГОТОВ (коммит `bcb3df1`)
Разблокирована регистрацией аккаунта. Серверная верификация по MASTER 7.4 / 12.1 / 6.1#1.
- **`server/src/services/apple-auth.service.js`** (новый) — `authenticateWithApple({ identityToken, fullName })`: `appleSignin.verifyIdToken(identityToken, { audience: config.apple.clientId })` через Apple JWKS (глобальный fetch Node 20), извлечение `sub`→appleUserId и `email`, поиск/создание User, выдача JWT. Зеркало `google-auth.service.js`.
  - email приходит ТОЛЬКО при первой авторизации и может отсутствовать (private relay скрыт) → при отсутствии поле email НЕ пишется (иначе конфликт sparse unique индекса). При наличии email — линк к существующему аккаунту (email/google).
  - `fullName` приходит от клиента ТОЛЬКО при первой авторизации (в identityToken имени нет).
- **`server/src/routes/auth.js`** — добавлен `POST /api/auth/apple` (Zod: identityToken required; authorizationCode/fullName optional). authorizationCode в теле принимается, но обмен кода НЕ делаем (для MVP не требуется, .p8-ключ не нужен). Невалидный токен → `AUTH_APPLE_FAILED` (401).
- **`server/package.json`** — `+ apple-signin-auth ^2.0.0` (CommonJS, Node ≥18; версия и API verifyIdToken проверены перед написанием — уроки #21/#22).
- User-схема НЕ менялась (`appleUserId` unique sparse + `authProvider:'apple'` уже были).
- Синтаксис обоих файлов проверен `node --check` ✅. **Тест end-to-end — только на реальном устройстве через TestFlight (1.8/Фаза 7):** Apple Sign In в симуляторе не работает; локально проверяется лишь верификация (невалидный токен → 401).
- ⚠️ **Юзеру на сервере:** `cd ~/Chitatel_app/server && npm install` (поставить apple-signin-auth) перед `npm run dev`.

---

## ЗАДАЧА 1.7 (APPLE SIGN IN — ФРОНТ) + 1.8 (ГОСТЕВОЙ РЕЖИМ) — ✅ ЗАВЕРШЕНО 12.06.2026

Всё в `main`. Тест Apple Sign In — на устройстве (Фаза 7); гостевой режим — в симуляторе. Перед началом проверено фактическое состояние репо (а не только контекст-файлы): связка Apple-фронта была НЕ начата (кнопка = заглушка-snackbar), стейт-машина guest частично уже была, бэкенд гостя готов с Фазы 2.

### 1.7 Apple Sign In, Flutter-сторона (часть задачи 1.7)
Связка «кнопка → токен Apple → бэкенд» по образцу Google. Бэк (`POST /api/auth/apple`, 1.3) был готов.
- `app/pubspec.yaml` (`ff49804`) — `+ sign_in_with_apple: ^6.1.4`. **Версия запиннена под тулчейн:** 8.0.0 требует Flutter 3.41+, 7.x таргетит Swift 6/Xcode 16; машина — Xcode 15.2/Flutter 3.22.3. Ветка 6.1.x: min Flutter 3.19.0, поддерживает Xcode 15 И 16 (6.1.3 — «Support Xcode 16 in addition to all previously supported versions»). `^6.1.4` = >=6.1.4 <7.0.0. Версия/совместимость сверены по pub.dev (страница + changelog), не по памяти — урок #27.
- `app/lib/features/auth/services/auth_service.dart` (`a4c48a5`) — `appleSignIn({identityToken, fullName?})` → POST /api/auth/apple. fullName шлём только если непустой (Apple отдаёт имя один раз).
- `app/lib/features/auth/providers/auth_provider.dart` (`49f9f8c`) — `signInWithApple()`: `SignInWithApple.getAppleIDCredential(scopes:[email,fullName])`, собирает fullName из givenName+familyName, шлёт на бэк, сохраняет токены. Отмена входа (`AuthorizationErrorCode.canceled`) → молча в гостевой режим (как Google при account==null).
- `app/lib/features/auth/screens/login_screen.dart` (`9f3b447`) — кнопка Apple вызывает `signInWithApple()` (была заглушка-snackbar «будет доступно»).
- ⚠️ **Xcode (юзер, один раз):** Runner → Targets → Runner → Signing & Capabilities → + Sign in with Apple (добавит `com.apple.developer.applesignin` в Runner.entitlements). Без capability вход падает БЕЗ видимой ошибки (требование пакета). App ID в портале capability уже имеет.
- ⚠️ **Юзеру:** `flutter pub get` + `cd ios && pod install` (нативный пакет). Тест — реальное устройство/TestFlight (в симуляторе Apple Sign In не работает).

### 1.8 Гостевой режим + локальный прогресс (Apple Guideline 5.1.1(v))
Apple требует доступ к бесплатной функциональности без аккаунта. Половина была готова раньше (стейт-машина guest: `AuthStatus.guest`, `enterAsGuest()`, `checkAuth()`→guest, `logout()`→guest). Бэкенд под гостя готов с Фазы 2 (роуты каталога `/books`,`/featured`,`/search`,`/books/:id` без auth; аудио через `optionalAuth` — бесплатные книги и превью открыты гостю, платное → 403). Дописан фронт:
- **Заметная опция «Продолжить без регистрации»** (`login_screen.dart`, `4689adc`) — была тусклая ссылка «Пропустить» (textTertiary), теперь terracotta + w600. Логика та же (enterAsGuest + onboarding_seen=true + go home). Apple-ревьюер должен видеть guest-вход.
- **Локальный прогресс гостя** (`progress_service.dart`, `3af0407`) — ProgressService теперь инжектит SecureStorage: гость (нет токена) → read/write в SharedPreferences (`guest_progress_<bookId>`); авторизован → сервер (как было). **Фикс бага «позиция не запоминается»** — раньше POST гостя уходил на сервер, получал 401 и тихо терялся (урок #10). Накопление totalListenedSeconds/частей повторяет серверное правило (та же часть + движение вперёд). Авто-миграции guest→сервер при логине НЕТ (PENDING — на будущее).
- **GuestGate** (`shared/widgets/guest_gate.dart`, `cd27113`, новый) — реюзабельный гейт: авторизован → реальный экран; initial/loading → лоадер (чтобы авторизованный не увидел приглашение на кадр); гость → мягкое приглашение «Войти или зарегистрироваться» (→ push /login). НЕ тупик — бесплатные табы доступны через нижнюю навигацию.
- **Роутер** (`app_router.dart`, `421e168`) — GuestGate обёрнут вокруг табов **Клуб** и **Профиль** (требуют аккаунт). Главная/Каталог/Поиск/Плеер бесплатных книг — открыты гостю.

### Apple-доступность (что закрывает 5.1.1(v))
- Гость не форсится регистрироваться при старте: видимая опция «Продолжить без регистрации».
- Бесплатные разборы (Маленький принц) реально работают без аккаунта, позиция запоминается локально.
- Зоны с аккаунтом (Клуб/Профиль) мягко приглашают войти, а не падают/молчат.
- Покупка платных за гостя → логин-гейт прилетит вместе с Фазой 3 (флоу покупки ещё не построен) — НЕ scope 1.8.

---

## АКТУАЛЬНЫЕ sha ФАЙЛОВ (всё в `main`)

**Apple Sign In фронт (1.7) + гостевой режим (1.8) — 12.06.2026:**
- `app/pubspec.yaml` — `0652bea44ab0c7b1eca7c3bf1f2affc514ea60d9` (+sign_in_with_apple ^6.1.4) — `ff49804`
- `app/lib/features/auth/services/auth_service.dart` — `7ea333f1ca0f7930dd02c1ff403d4a8e96ed681f` (+appleSignIn) — `a4c48a5`
- `app/lib/features/auth/providers/auth_provider.dart` — `dc4c0004b60cc2d30a958ecac654eca27d21cefa` (+signInWithApple) — `49f9f8c`
- `app/lib/features/auth/screens/login_screen.dart` — `e308e379c7e1539b5f3545cf8f4f9a2e52eb216c` (Apple-кнопка + «Продолжить без регистрации») — `9f3b447`+`4689adc`
- `app/lib/shared/widgets/guest_gate.dart` — `78596cc0894a74d18056fe47fea92235f5fd893f` (новый, GuestGate) — `cd27113`
- `app/lib/core/router/app_router.dart` — `2f1f05ad224f106f0508327b9439295794ae949f` (GuestGate на Клуб/Профиль) — `421e168`
- `app/lib/features/player/services/progress_service.dart` — `d048f768de6976b515a913d872747ed02922b62e` (локальный прогресс гостя) — `3af0407`

**Auth / задача 1.3 (коммит `bcb3df1`, 11.06.2026):**
- `server/src/services/apple-auth.service.js` — `9b1ed173e8c88b124a267ae7f194edf14dba5a3a` (новый: Apple Sign In, verifyIdToken JWKS, зеркало google-сервиса)
- `server/src/routes/auth.js` — обновлён, +`POST /api/auth/apple` (коммит `bcb3df1`)
- `server/package.json` — обновлён, +`apple-signin-auth ^2.0.0` (коммит `bcb3df1`)

**Бэкенд (4.12 + всё предыдущее, на 19.05.2026):**
- `server/src/routes/club.js` — `16137589b3078ec9f7d028190b4b495b92d4746c` (context + mentionable + sanitizeMentions + pin + read + `POST /chat/voice` + антиобход type=voice)
- `server/src/models/ChatMessage.js` — `4811ac13ae233903f050bc92ede44093d0381d74` (v5 + imageStoragePath + voiceStoragePath)
- `server/src/services/voice.service.js` — `c937715cda3c48c1993f22978a81f759b64b2e5e` (ALLOWED_MIME audio/mp4|m4a|aac→m4a, MAX_DURATION_SEC=180, MAX 4МБ, generateVoiceSignedUrl /audio/→/voice/)
- `server/src/routes/voice.js` — `32fd15b8f5503b42bd68049173b1269420825df5` (GET /voice/<file>?exp&sig с Range 206, path-traversal, VOICE_URL_EXPIRED/VOICE_NOT_FOUND)
- `server/src/app.js` — `48b24d6ea6db43ec01bb17e76ffc95b063c5d669` (+voiceRoutes на /voice)
- `server/src/services/image.service.js` — `49a193adbf32070a1577f890544736dadf3b5ddb` (не менялся)
- `server/src/routes/images.js` — `6de2df262baf1d35d41b6c556382d40ab3a81d0d` (не менялся)
- `server/src/routes/books.js` — `d655768fc6234f9c1cc36adad2f6a825b3ea3fe4` (гость-доступ к каталогу + optionalAuth на аудио — основа бэкенда 1.8, с Фазы 2)

**Фронт чат (`app/lib/features/club/`):**
- `widgets/chat_tab.dart` — `67496f4b1b27b947ef4e31bd4c896b0155bd04d8` (коммит `f7d2e7f`: 4.9-4.12 интегрированы; real-time фикс `49b5b41`)
- `widgets/chat_message_bubble.dart` — `143405a1356650bc9df9e9fb3ff0e522b082e738` (коммит `5054bd2`: VoicePlayer + @подсветка + Закрепить)
- `widgets/chat_input.dart` — `d4ea37b8e979a5d866fa52e89da669a423300840` (коммит `c5f5d17`: Expanded VoiceRecorder при записи; автокомплит @)
- `widgets/voice_recorder.dart` — `da2a7e17ee1969ab8d29c2c843d144b8492f7738` (коммит `d3be72a`: record 4.x API + onRecordingStateChanged)
- `widgets/voice_player.dart` — `4875b87b3966498acc7c92af2afa9a44a4e7ae67` (коммит `db8a1c4`: just_audio + waveform + seek)
- `services/club_api_service.dart` — `a95f5b2b1161a92b757ebb4edf8984d0a86a1b49` (sendVoiceMessage + mentionable/pin/read/context)
- `core/network/api_endpoints.dart` — `4f5cb8e9a999034f3019d5e95581321cee94b9f0` (clubChatVoice/mentionable/read/pin/context + apple)
- `models/chat_message.dart` — `cb9e5318bb3cb6809a43f570d8133471117a19fb` (voiceUrl/Duration/Waveform; ReplySnapshot)
- `services/club_socket_service.dart` — `db25b770ffdac3c6daf2167033c65ca7888d98a3` (sealed events — не менялся)
- `app/ios/Runner/Info.plist` — `38773b3207cf1fc2378121b2fc89153fa63ad432` (+NSMicrophoneUsageDescription)

---

## ХРОНОЛОГИЯ КОММИТОВ (все в `main`)

**Apple Sign In фронт (1.7) + гостевой режим (1.8) — 12.06.2026:** `ff49804`(pubspec +sign_in_with_apple ^6.1.4) `a4c48a5`(auth_service appleSignIn) `49f9f8c`(auth_provider signInWithApple) `9f3b447`(login_screen Apple-кнопка) `cd27113`(guest_gate) `421e168`(router GuestGate Клуб/Профиль) `3af0407`(progress_service локальный прогресс гостя) `4689adc`(login_screen «Продолжить без регистрации»).

**Apple Sign In 1.3 (бэк):** `bcb3df1`(apple-auth.service + POST /api/auth/apple + apple-signin-auth).

**Фаза 4 чат:** Бэк 4.9/4.10/4.11: `aaa7a8d`. Фронт 4.9/4.10/4.11: `252aefb` `933bca0` `3b50dc8` `cd4bbc4` `37e6fd9`. Real-time фикс: `49b5b41`. Бэк 4.12: `3057645` `e2694a4` `ee7367b` `0b64f78` `6017ab5`. Фронт 4.12: `a95f5b2` `6ef83c4`→`6640d63` `4875b87`/`db8a1c4` `5054bd2` `53324f5` `f7d2e7f` `4f8a92c`. Фикс record: `814a7e9`(битый override — откачен) `611bd26`(откат) `c8bb44d`(record ^4.4.4) `b1ecc01`(voice_recorder под 4.x). Layout-фикс: `d3be72a` `c5f5d17`. Контекст-файлы: `743d854`(создан AI-CONTEXT-2.md).

---

## ИНСТРУКЦИЯ ЮЗЕРУ — ЗАПУСК И ТЕСТ

### Запуск после правок
```bash
cd ~/Chitatel_app && git pull origin main
cd server && npm install           # ⚠️ ПОСЛЕ 1.3: поставить apple-signin-auth (если ещё не ставил)
cd server && npm run dev           # рестарт если новые роуты
cd ../app && flutter pub get       # ⚠️ ПОСЛЕ 1.7: подтянуть sign_in_with_apple
cd ios && pod install && cd ..     # ⚠️ sign_in_with_apple нативный — pod install ОБЯЗАТЕЛЬНО
flutter run                        # полный рестарт (новый нативный пакет), не hot reload
```
⚠️ **Xcode один раз (для Apple Sign In):** Runner → Targets → Runner → Signing & Capabilities → «+ Capability» → **Sign in with Apple**. Иначе вход падает без видимой ошибки.

### Тест 1.7 / 1.8
- **Apple Sign In** — ТОЛЬКО на реальном устройстве/TestFlight (в симуляторе не работает). Локально — только компиляция.
- **Гостевой режим** (в симуляторе): на экране входа — «Продолжить без регистрации» → главная как гость. Открыть бесплатный разбор (Маленький принц), послушать, выйти-зайти → позиция запомнилась (локально). Табы Клуб/Профиль → приглашение «Войти или зарегистрироваться». Главная/Каталог/Поиск — открыты.

### Тестовые аккаунты (нет штатного логаута — между ними Device→Erase All Content)
| Email | Пароль | Роль |
|---|---|---|
| anna@chitatel.app | anna123456 | admin (голосовые, закрепы, mentions-цель, ссылки) |
| test-premium@chitatel.app | test123456 | premium активный клуб (основной тест участницы) |
| test-basic@chitatel.app | test123456 | basic |
| test-expired@chitatel.app | test123456 | архив read-only |

Seed: `cd ~/Chitatel_app/server && npm run seed:club`.

### ⏳ Чек-лист НЕ верифицированного отдельно (добегать при следующем заходе в чат)
Фаза 4 функционально завершена (голосовые+фото подтверждены 19.05), но эти пункты юзер на последнем тесте явно не называл — стоит проверить при случае, НЕ блокер:
**Под `anna@chitatel.app`:** 4.10 закрепы (long-press → баннер → переход), 4.9 mentions (@ → Анна → подсветка), real-time (удаление сразу, не залипает после диалогов/табов).
**Под `test-premium@chitatel.app` (Erase сначала):** микрофона НЕТ, нет пункта «Закрепить», голосовые Анны играют, `@Анна` работает.
**Новое (1.7/1.8):** Apple Sign In на устройстве; гостевой режим в симуляторе (см. выше).

Клавиатура в симуляторе не всплывает = Hardware Keyboard (Cmd+Shift+K), НЕ баг (урок #19).

---

## УРОКИ (продолжение, #20+; #1-19 в AI-CONTEXT.md)

**#20 ⚠️ КОНТЕКСТ-ФАЙЛ РАЗРОС — ДЕЛИТЬ, НЕ ПЕРЕПИСЫВАТЬ (17.05.2026):** при превышении ~50KB AI-CONTEXT.md заводится AI-CONTEXT-2.md (продолжение). Первый файл НЕ редактируется (стабильный фундамент-архив), прогресс фиксируется во втором. AI читает оба по порядку. Юзер должен добавить упоминание второго файла в Project Instructions проекта Claude. **(12.06: AI-CONTEXT-2 приближается к ~50KB — при следующем крупном блоке завести AI-CONTEXT-3 по той же схеме.)**

**#21 ⚠️ НЕ УГАДЫВАТЬ ВЕРСИИ ПАКЕТОВ (17.05.2026):** Claude выдумал `record_platform_interface 1.0.4` (не существует) — `pub get` упал. Правильно: либо цельный пакет (перешли на `record 4.x`), либо подбор по фактическому выводу `flutter pub get`/`flutter pub deps`. **(11.06: при 1.3 версия `apple-signin-auth@2.0.0` и API проверены через npm registry перед написанием. 12.06: при 1.7 версия `sign_in_with_apple ^6.1.4` подобрана по pub.dev — урок применён.)**

**#22 ⚠️ API ПАКЕТА МЕНЯЕТСЯ МЕЖДУ МАЖОРАМИ:** record 5.x (`AudioRecorder()`, `RecordConfig`, `sampleRate`) ≠ record 4.x (`Record()`, параметры в `start()`, `samplingRate`). При смене мажорной версии — переписать вызовы под фактический API целевой версии.

**#23 ⚠️ RenderFlex unbounded width (17.05.2026):** виджет с `Spacer()`/`Expanded` внутри `Row`, помещённый в родительский `Row` без `Expanded`, падает. Решение — растягивающийся виджет сообщает родителю состояние через колбэк, родитель оборачивает в `Expanded`. Единый инстанс виджета через переменную. **Фикс подтверждён тестом 19.05.**

**#24 ⚠️ ПРИЧИНА БАГА ИЗ СТЕКТРЕЙСА, НЕ ИЗ ДОГАДКИ (17.05.2026):** layout-баг и конфликт record — оба раза причина установлена из фактического вывода (стектрейс / текст ошибки pub), потом фикс. Подтверждает урок #17.

**#25 ⚠️ ИНФРАСТРУКТУРНЫЕ/ВЛАДЕЛЬЧЕСКИЕ ВОПРОСЫ — РАЗДЕЛЯТЬ ДОСТУП И ВЛАДЕНИЕ (19.05.2026):** активы проекта (Apple/домен/сервер) на владельце (Анна/ИП), разработчику — делегированный техдоступ (App Store Connect Users&Access; Namecheap Share Access; SSH-ключ на Contabo). «Дать пароль» = передача владения + ломается о 2FA + против ToS. **(11.06 применено: разработчик добавлен Admin под своим Apple ID.)**

**#26 ⚠️ СВЕРЯТЬ ФАКТИЧЕСКИЕ ХАРАКТЕРИСТИКИ ЖЕЛЕЗА (11.06.2026):** машина значилась «2018», по факту **2017** (упирается в Ventura; 2018 тянул бы до Sequoia). Из-за разницы менялся весь вывод про сборку → App Store только через облачный CI. Перед инфра-решениями уточнять реальную под-модель/год/RAM; Apple-требования проверять web-поиском по офиц.источникам.

**#27 ⚠️ ВЕРСИЯ ПАКЕТА — ПОД ТУЛЧЕЙН, НЕ ПОСЛЕДНЯЯ (12.06.2026):** для `sign_in_with_apple` взяли `^6.1.4`, не latest. 8.0.0 требует Flutter 3.41+; 7.x таргетит Swift 6 (Xcode 16) — а машина Xcode 15.2/Flutter 3.22.3. Ветка 6.1.x: min Flutter 3.19.0, поддерживает Xcode 15 И 16 (6.1.3 «Support Xcode 16 in addition to all previously supported versions»). Версию/мин-SDK/мин-Xcode сверял по pub.dev (страница + changelog), не по памяти. **Перед добавлением нативного пакета проверять: (1) мин Flutter SDK, (2) мин Xcode** — машина застряла на 15.2, новый пакет с требованием Xcode 16 сломал бы локальную сборку ВСЕГО приложения, а не только своей фичи. Продолжение #21/#22/#26.

---

## РАБОЧАЯ СРЕДА (детали в AI-CONTEXT.md; машина уточнена 11.06.2026)

**Машина — MacBook Pro 13″ 2017 (2×Thunderbolt 3), Intel Core i5 2.3 GHz (2 ядра), 8 GB RAM. Потолок macOS — Ventura (13), уже стоит; выше не поднимается (Sonoma/Sequoia/Tahoe требуют 2018+).** ⚠️ В AI-CONTEXT.md ошибочно записано «2018» — корректно «2017» (первый файл не правим, см. урок #26 и секцию «РЕШЕНИЯ ПО СБОРКЕ И МАШИНЕ»). Xcode 15.2 + iOS 17.2 sim (iPhone 15), Flutter 3.22.3 (withOpacity), Node 20.20.1, MongoDB 7.0.20 (`mongod --dbpath ~/mongodb/data`). Аудио: `/Users/g/Chitatel_app/audio-storage/` (club-images/, voice-messages/). Бэк `cd ~/Chitatel_app/server && npm run dev`. Фронт `cd ~/Chitatel_app/app && flutter run`. pod install: 2 предупреждения [!] безвредны. Нет штатного logout — Device→Erase All Content.

---

## ОТЛОЖЕНО (PENDING)

- **Плеер Маленького Принца:** `PlatformException(-11800)` AVFoundation iOS на сетевой MP3, вероятно баг симулятора. Отложено до физустройства/VPS. Test ID `6a0347c62598cd8bc4f96df8`, bookSlug `malenkii_princ`.
- **Сборка/релиз (toolchain):** машина 2017 не поднимет Xcode 26 → App Store/TestFlight только через облачный CI **Codemagic** (бесплатно 500 мин/мес, без карты не спишут, подпись через App Store Connect API key). Подробности — секция «РЕШЕНИЯ ПО СБОРКЕ И МАШИНЕ». Нужно к Фазе 7, сейчас НЕ делать. Mac mini M1 (~$350–450) — апгрейд при упоре в потолок разработки.
- **Задача 1.8 «Гостевой режим»** — ✅ ГОТОВА 12.06.2026 (заметная опция входа без регистрации + локальный прогресс + GuestGate на Клуб/Профиль). Закрывает Apple 5.1.1(v) в части скоупа MVP.
- **Авто-миграция гостевого прогресса на сервер при логине** — НЕ делали (минимальный код). Гость пишет локально; при логине стартует с серверного прогресса. Перенос локальной позиции в аккаунт — опционально позже (например, при первом логине слить `guest_progress_*` → POST /api/progress). НЕ блокер для Apple.
- **Логин-гейт на покупку платных за гостя** — прилетит с Фазой 3 (флоу покупки ещё не построен). Сейчас вне scope.
- **Чек-лист 4.9/4.10/4.11/real-time/участница** — не верифицирован отдельно. Не блокер, добегать при следующем заходе в чат.
- **Вопросы к Анне:** 6.1 (пакеты «Достоевский» эксклюзив?/2.5.5), 8.1 (отзывы в v1?/6.9), 8.3 (когда MP3 остальных разборов?), 8.5 (когда Анна активно отвечает в чате?/релиз), NEW (обложка клуба как книга или арт?/схема ClubMonth+переключатель).
- **Визуальный переключатель клубов** — не дорабатывать пока Анна не ответит про обложку/название клуба.
- **STEP-BY-STEP.md не обновлён под 4.6-4.12 / 1.7 / 1.8** — план зафиксирован здесь, не критично.

---

## ДАЛЬШЕ ПО ПЛАНУ

```
Фаза 4 ✅ ЗАВЕРШЕНА (19.05.2026) — все 12 задач + навигация + фиксы. Голосовые+фото подтверждены тестом
1.3 Apple Sign In бэк ✅ (11.06) · 1.7 Apple Sign In фронт ✅ (12.06) · 1.8 гостевой режим ✅ (12.06)
  → Фаза 5 (ИИ-дневник: 5.1, 5.2, 5.3)  ← СЛЕДУЮЩАЯ
  → Фаза 3 (платежи) — здесь же Small Business Program 15% + Tax/Banking анкета + логин-гейт на покупку
  → Фаза 6 (полировка) — включая push (фича #8 чата)
  → Фаза 7 (TestFlight + App Store) — сборка через Codemagic (машина 2017 не тянет Xcode 26)

🚨 КРИТИЧНО ПЕРЕД TESTFLIGHT: 1.8 (guest mode) ✅ готов; ITSAppUsesNonExemptEncryption=false в Info.plist;
   capability «Sign in with Apple» в Xcode; сборка через облачный CI (Codemagic)
```

---

*Обновлён: 12.06.2026. Продолжение AI-CONTEXT.md (тот разросся 52KB, не редактируется). **Фаза 4 ЗАВЕРШЕНА.** **НОВОЕ 12.06: задачи 1.7 (Apple Sign In, Flutter-сторона) и 1.8 (гостевой режим, Apple 5.1.1(v)) ✅ ЗАВЕРШЕНЫ и в `main`.** 1.7: кнопка Apple вызывает signInWithApple() (была заглушка), пакет sign_in_with_apple ^6.1.4 запиннен под Xcode 15.2/Flutter 3.22.3, шлёт identityToken+fullName на POST /api/auth/apple (бэк 1.3). 1.8: заметная «Продолжить без регистрации»; локальный прогресс гостя в SharedPreferences (фикс бага «позиция не запоминается» — был тихий 401); GuestGate (мягкое приглашение войти) на Клуб/Профиль; бесплатный контент открыт гостю; бэкенд гостя был готов с Фазы 2. Тест Apple Sign In — на устройстве (Фаза 7); гостевой режим — симулятор. Юзеру: flutter pub get + pod install + capability «Sign in with Apple» в Xcode. Урок #27 (версия пакета под тулчейн, не последняя). Следующее — Фаза 5. Прогресс фиксировать далее ТОЛЬКО здесь; при следующем крупном блоке — завести AI-CONTEXT-3 (файл близок к ~50KB).*
